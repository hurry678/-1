# 第三问：压力感知微批、时隙预约与防死锁控制

## 1. 问题与模型选择

附件7包含600项高密动态任务，释放跨度为1797 s，平均约每3 s到达一项任务。系统只有20辆OHT。任务涉及385个不同Carrier、129个重复Carrier，最长Carrier链为9，对应215条前序边。

严格按照全过程计划，第三问正式采用“压力感知微批调度＋区域压力平衡＋有限时域时隙预约＋空间接纳＋等待图防死锁”。直接复用第二问滚动插入仅作为基准，不作为正式结果。

## 2. 高密动态取送模型

记当前可见微批任务为 \(J_t\)，车辆为 \(K\)，任务释放时刻、优先级和起终Port为 \(r_j,p_j,s_j,d_j\)。每项任务唯一分配：

\[
\sum_{k\in K}x_{jk}=1.
\]

若同一Carrier存在前序 \(i\prec_c j\)，则

\[
A_j\ge r_j,\qquad A_j\ge C_i.
\]

车辆一次最多载一个Carrier，取货和放货各占用Port 8 s。调度器只接收已经释放并解除Carrier前序阻塞的任务ID，未释放任务的OD、Priority和Carrier字段不可见。

## 3. 区域压力

将轨道按Link下游节点编号每10个节点划分为稳定区域。区域压力定义为

\[
\Pi_g=N_g^{task}+0.55N_g^{veh}
+8\frac{L_g^{occupied}}{L_g^{total}}
+1.2W_g^{resource}-0.35N_g^{idle}.
\]

其中任务压力由当前可见的待分配任务和车辆承诺队列共同形成；车辆密度按每辆车占用1250 mm参考空间估计；资源等待只使用当前和历史状态。任务调入高于平均压力区域时增加边际惩罚。

## 4. 压力感知微批拍卖

普通任务每5 s或当前可分配任务累计到6项时联合处理；优先级不低于90的任务进入即时通道。对任务、车辆和队列插入位置枚举报价：

\[
\begin{aligned}
b_{jkq}={}&\Delta\sum_h\widehat C_h
+\lambda_F\frac{\widehat C_j-r_j}{w(p_j)}
+\lambda_D\Delta D_{empty}\\
&+\lambda_Q|Q_k|^2
+\lambda_P P_j
+\lambda_B\Delta\Pi_g
+\lambda_S SlotConflict_j
+\lambda_L LateRisk_k.
\end{aligned}
\]

程序按优先级、等待年龄和释放时刻处理微批，逐项选择最小报价并即时更新剩余任务的车辆队列状态。

空闲OHT仍在轨道上合法巡航。若某个新任务Source距离短于当前速度的离散制动接纳距离，则该车辆候选施加大罚值，避免产生不可实现的急停。

## 5. 有限时域时隙预约

车辆每次生成前往Source或Destination的路径时，对未来路径上的MERGE、CurveGroup和目标Port计算预计到达时刻，并在对应资源日历中插入互不重叠的计划时隙：

\[
[s_{ir},e_{ir})\cap[s_{jr},e_{jr})=\varnothing.
\]

时隙间隔为0.2 s；Port持续8 s，MERGE和Curve时隙按车辆参考间距及710 mm/s巡航速度计算。每1 s滚动补充进入前视范围的新资源。

计划时隙用于报价中的冲突和迟到风险，并记录计划—实际偏差。实际放行继续遵守物理队首、载货状态、优先级和资源容量，防止预测时隙把权限错误授予不可超车队列的后车。

本次生成4580个计划时隙，其中4444个实际使用，226个因到达偏差滚动平移，其余为路径阶段切换后取消或未触发时隙。独立审计确认所有实际资源使用时段重叠数为0。

## 6. 等待图与恢复机制

每1 s对静止且处于跟驰、MERGE、Curve或Port等待的车辆建立等待图：若车辆 \(u\) 等待车辆或资源所有者 \(v\)，建立边 \(u\to v\)。只有同一个环连续出现5次且等待超过10 s，才认定为持续死锁风险。

恢复动作限定为：撤销环中最低优先级、未载货车辆的未生效预约，并重新计算时隙。已经进入资源或已取货车辆绝不撤权、转交、瞬移或删除。本次正式仿真等待图最大6条边，没有形成持续环，因此恢复次数为0。

## 7. 交通与运动控制

仿真步长为0.2 s，执行：

- Link限速及710 mm/s保守巡航上限；
- 加速度不超过2000 mm/s²，减速度不超过3000 mm/s²；
- 车长950 mm、净空300 mm，即同Link参考点间距至少1250 mm；
- Port独占服务；
- MERGE、CurveGroup容量均为1；
- 短Link前跨Link预约和安全制动；
- 空闲车辆仍参与跟驰和资源仲裁。

## 8. 基准与消融结果

| 方案 | 平均任务执行时间/s | Makespan/s | 决策数 | 硬违规 | 未来泄漏 |
|---|---:|---:|---:|---:|---:|
| 直接复用第二问滚动插入 | 834.963753 | 4354.0 | 598 | 0 | 0 |
| 5 s普通微批 | 854.261420 | 4534.0 | 445 | 0 | 0 |
| 3 s压力微批，无显式时隙 | 847.958087 | 4091.2 | 547 | 0 | 0 |
| 文档完整主模型 | 856.908420 | 4231.8 | 448 | 0 | 0 |

压力微批显著降低Makespan；无时隙压力方案相对直接滚动缩短6.036%。完整主模型相对直接滚动将Makespan缩短2.807%，但平均任务执行时间增加2.628%。这说明本数据下压力和预约机制改善总体清空速度，却增加了部分任务等待。根据本次“按照文档生成第三问”的要求，正式结果采用文档完整主模型，并如实保留性能权衡。

## 9. 正式指标

| 指标 | 数值 |
|---|---:|
| TaskCount | 600 |
| AvgTransferTime | 856.908420 s |
| AvgAssignTime | 243.219087 s |
| AvgPickupResponseTime | 508.808667 s |
| AvgTransportTime | 88.880667 s |
| AvgPausingTime | 3.775333 s |
| Makespan | 4231.8 s |
| AvgEmptyDistance | 13256.484500 mm |
| AvgTotalDistance | 73875.018334 mm |
| SimulationRuntime | 9.494679 s |

## 10. 验证结论

完整轨迹共423,200行，对应21,160个连续仿真步，每一步恰有20辆OHT。程序内部审计与独立验证均通过：

- 600/600任务完成；
- 215条Carrier前序边满足；
- 448次在线决策未来信息泄漏为0；
- 4580个计划时隙、4444个实际使用时隙，实际重叠为0；
- 5245条底层资源预约事件完整；
- 最小同Link参考点间距为1250 mm；
- 最大速度710 mm/s，加减速度满足附件参数；
- 交通、资源和位置硬违规均为0；
- Excel、CSV、JSON和完整轨迹一致。

因此，该版本完整落实了总计划第三问的微批、压力、时隙、空间接纳、防死锁和统一审计思路。
