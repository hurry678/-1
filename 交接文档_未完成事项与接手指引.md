# 北极边缘冰区数学建模 —— 交接文档（最终状态与接手指引）

> 更新：2026-08-08 15:10（Codex 主控）
> 用途：记录最终完成状态、遗留可选事项、关键决策与接手指引。

## 1. 最终状态

> 2026-08-08 17:50 修订：按用户要求整篇重写论文（学习其 2026 杭电校赛论文
> 的章节排布与写法），新增：
> - 去掉全部"教师指定"类措辞，改为题目简化方向/本文选择；
> - 每一问新增"模型与算法选择+选型理由"，并补充对比实验：
>   问题一 M0/M_I/M_C/M1 归因对比；问题二 未优化/时间预测+Anderson/+ILU
>   三档对照与 GMRES/BiCGSTAB 线性求解器对照；问题三 网格搜索/Nelder--
>   Mead/差分进化三算法对比（差分进化独立复现网格搜索最优 (0.015,0.5)，
>   J=636.2，`output/problem3/problem3_de_summary.json`）；问题五 时变
>   场/静态场/5-10 min 离散三方案对照（`output/problem5/route_result_
>   static_24h.json`）；
> - 新增"五大模型/十大算法"体系映射（参考《必须掌握的五大模型十大算法
>   （适应于研究生）简析》），含问题-模型-算法-检验映射表；
> - 新增 14 条真实文献引用与 AI 工具使用说明（附录 B）。
> 论文现为 17 页：`paper/main_new.pdf`（2026-08-08 19:29 编译，含全部
> 核验订正；`paper/main.pdf` 被 WPS 占用待替换，见 8.6 节）。

| 问题 | 状态 |
|---|---|
| 问题一（M1 简化与量级分析） | ✅ 完成（含 Q2-R2 删项归因数据） |
| 问题二（冰水耦合数值求解） | ✅ 40×20 与 100×50 全部验收；200×100 算力受限，论文已如实说明 |
| 问题三（C_d、α 反演） | ✅ 真实反演 + 孪生试验 + 40×20 验证 |
| 问题四（初始冰厚反演 + 48h 预报） | ✅ 反演 + 48h 验证 |
| 问题五（LNG 航线优化） | ✅ 72h 预报 + 时变 Dijkstra 航线 |
| 论文 | ✅ paper/main_new.pdf（17 页，xelatex 编译通过，无占位符；交付 zip 已含同版） |

## 2. 关键产物路径

- 论文 PDF：`paper/main_new.pdf`（待替换 `paper/main.pdf`）；源码 `paper/main.tex`
- 数据附件包：`研训188队_数据附件.zip`（论文 + 各问题结果 JSON/CSV/图 +
  交接文档）
- 主控看板：`全题主控协作看板.md`
- 问题一正式章节：`问题一_正式章节片段.tex`
- 问题二：
  - 24h 复验与签署：`问题二_M1_24h复验_第四轮_编程手交付与主控签署.md`
  - 100×50 验收：`output/problem2/v2_standard_grid/q2r4_acceptance.json`
  - 迭代预算扩展批准：`问题二_100x50迭代预算扩展_建模手批准.md`
- 问题三：`问题三_建模手与编程手交付.md`、`output/problem3/`（real/twin/verify）
- 问题四：`output/problem4/problem4_inversion.json`、`problem4_verify.json`、
  `verification_40x20_48h/`
- 问题五：`output/problem5/forecast_40x20/`、`route_result_tau5.json`、
  `route_tau5.png`
- 测试：`python -m pytest tests -q` → 83 passed

## 3. 遗留可选事项（不阻塞交付）

1. 若未来有更强算力（或在非限速环境），运行题定 200×100：
   `python 问题二_M1_标准网格_第五轮.py --grid 200x100 --mode M1`
   （实测单步 305–1046 s，全时段预计 2–6 天；本机 `Start-Process` 后台进程
   被限速至约 15% CPU，前台 shell 为 100%，因此本机不可行），完成后运行
   `python 问题二_Q2R5_网格限制比较.py` 并替换论文中的 100×50 口径。
   **最新状态（2026-08-08 22:40）**：已做求解器优化并重启该运行：
   - 优化项（均保持 M1 固定点，40×20、6 h 等价性最大场差 0.025%<0.1% 门）：
     (a) 线性求解器 GMRES→BiCGSTAB；(b) 内层受保护 Anderson（P4，深度 3）；
     (c) 受保护线搜索（ω∈{0.5,1.0}）。
   - 效果（40×20、6 h）：墙钟 130→85 s；Picard 4504→2321（−48%）；
     GMRES 14387→3459（−76%）。87 项测试通过。
   - 200×100 前 5 步实测：255/259/230/302/360 s（原 305/743/1046/…），
     约 2.5–4.5× 提速；全时段预计约 1–1.5 天（前台满速）。
   - 运行方式：Windows 计划任务 `P2_200x100_M1`（包装脚本
     `C:\p2run\run_200x100.py`，日志 `C:\p2run\run.log`）。注意：本应用
     对一切脱离进程限速至约 10–16% CPU，因此后台预计 1–2 周；在普通终端
     前台执行同一命令约 1–1.5 天。监控方式：
     `schtasks /Query /TN P2_200x100_M1`；完成后结果在
     `output/problem2/v2_standard_grid/200x100/M1/run_1/`，随后运行
     `python 问题二_Q2R5_网格限制比较.py` 并替换论文口径。
2. 可选：问题四正则化敏感性补 κ_s=0.25/1.0 两档；
   问题三 NM 备选参数 (0.0076,1.498) 的敏感性小节。
3. 可选：问题五"静态 t=24h 场"对照方案（论文表已删该行，如需可补跑）。

## 4. 关键决策与已踩的坑（新接手必读）

- 教师订正口径（最高优先级）：内力一律 `∇·(h_i σ)`；M1 同时删除海冰
  惯性与海冰科里奥利，海水科里奥利保留；固壁观测误差放大处理；
  旧 round2/round3 结果不得作为正式证据。
- 求解器最终候选 P1+P2+P3（时间预测 + Anderson + ILU 复用）；
  迭代预算 reset=300、robust=500（100×50 困难窗口必需，测试已同步）。
- 200×100 结论：本机不可行，论文以 100×50 为正式标准网格并写明限制。
- 问题三：M1 系统性低估厚冰中心区北向速度 → 可辨识性弱；正式参数
  (C_d=0.015, α=0.5)，NM 备选 (0.0076, 1.498)；孪生证明算法有效。
- 问题五：终点 E 在 M1 预报中 A>0.6 全程禁行 → 航线终点取最近可通行网格
  (15750,250)（偏差 5408 m），论文已说明。
- 已知坑：中文路径在 `python -` stdin 会编码报错（用 .py 文件）；
  `Start-Process` 后需单独验证进程；驱动脚本对已存在输出目录抛
  FileExistsError（残留 run_1 先隔离）；问题五 Dijkstra 必须用状态最优
  到达时间（已修复，冒烟通过）。

## 5. 接手指引

```powershell
# 复核测试
python -m pytest tests -q
# 重编论文
cd paper; latexmk -xelatex main.tex
# 可选：200×100
python 问题二_M1_标准网格_第五轮.py --grid 200x100 --mode M1
```

## 6. 建议技能

- `handoff`（继续交接）、`doc-coauthoring`（论文定稿）、
  `literature-verifier`（如需文献核实）、`math-modeling-contest`（按需）。

## 7. 2026-08-08 19:10 核验记录（Codex 接手核验）

> 本轮对五个问题的模型选择、优化算法与论文证据链做了逐项核验，
> 数据源为 `output/` 下的验收 JSON 与 24h 复验汇总。

### 7.1 核验结论（全部通过，另订正 3 处口径）

- 问题一：五力量级 0.011463/0.064190/0.250000/1.281250/0.126491 N/m²、
  η_IC=4.5636% 与 `问题一建模思路.md` 一致；四模型归因表
  （M_I/M_C/M1 相对 M0 的四项差异、q=0.3105）与
  `output/problem2/diagnostics/v2_1/diagnostic_40x20_6h/` 完全一致。
- 问题二：求解器三档对照（504.787→420.081→395.899 s、加速 1.275、
  场差最大 0.0228%、耦合 6578→3547、Picard 17466→15221、GMRES
  54866→50433、重试 2+2→3+3）与
  `output/problem2/v2_24h_verification/24h_verification_summary.json` 一致；
  100×50 统计量与 `q2r4_acceptance.json` 一致。
- 问题三：三算法表（网格搜索/NM/DE 的最优参数、J、求值次数、墙钟、
  初值敏感性）与 `problem3_summary.json`、`problem3_de_summary.json`
  一致；孪生恢复误差与 `problem3_twin_summary.json` 一致。
- 问题四：48h 预报统计与 `problem4_verify.json` 一致。
- 问题五：时变/静态/时间离散对照与 `route_result_tau5/tau10/static_24h.json`
  一致；终点替代方案偏差 5408 m 与 JSON 一致。
- 文献：13 条参考文献全部在文中引用，且按首次引用顺序排列；无未引用条目。
- 编译：`python -m pytest tests -q` → 83 passed；
  `latexmk -xelatex` 重编译通过，17 页，无未定义引用/占位符。

### 7.2 本轮订正

1. 问题二表：40×20 中心点冰厚由 0.844804 订正为 0.844796
   （原值来自未优化基线 run，验收口径为优化 run 的 0.8447956）；
2. 问题四：正式方案改标为 κ_s=0（按总目标函数最小，与
   `problem4_inversion.json` 的 `selected_kappa_s=0.0` 及 48h 预报实际
   使用系数一致），κ_s=0.5 改列为带显式平滑的对照；摘要/结论中的
   “双正则化”表述相应改为“背景正则化+低频谱截断（显式平滑作敏感性对照）”；
3. 问题三：差分进化“独立收敛”改为“在各自预算内独立寻得”
   （DE 运行 success=false、8 代预算内命中与网格搜索相同的全局最优）；
4. 新增问题三孪生试验细网格目标函数等高线图
   （`paper/figures/problem3_twin_contour.png`，由
   `problem3_twin_summary.json` 的 fine_grid_cost_triples 重新生成，
   避免沿用标题误写为 “twin data” 的旧 `objective_contour.png`）。

### 7.3 遗留可选事项（不变）

- 更强算力时运行题定 200×100 并替换 100×50 口径；
- 问题四补 κ_s=0.25/1.0 两档；问题三 NM 备选参数敏感性小节；
- 注意 `问题三_参数反演.py` 的 `contour_figure()` 标题字符串写死为
  “(twin data)”，真实数据反演调用时图题仍会误标，重跑前需修正。

## 8. 2026-08-08 20:00 深度审计（结合原题再核验）

> 本轮不止对账，而是回到题目原文逐条重审模型与算法，新增五条结论：

### 8.1 公式（11）无根号——已用 PDF 渲染逐字形验证

- 题目 (11) 在 PDF 中实际排印为
  $V_s(t)=V_{\max}\cdot(1-A(x(t),y(t),t))$，**没有开根号**。
- 外层括号是 Word 公式对象转换成的矢量叶形符号，纯文本抽取会丢失，
  造成 `Vmax⋅` 与 `1−A(...)` 分开两行的错觉；
- 对照检验：同一 PDF 中 Δ 公式的根号渲染为“顶部钩+长横线+左竖”的完整
  根号，而 (11) 上方无横线、无竖杆，仅有成对镜像叶形括号；
- 结论：论文与 `问题五_航线优化.py` 的 $V_s=V_{\max}(1-A)$ 实现正确，
  无需改动；本记录供后人复核时参考，避免误改成 $\sqrt{1-A}$。

### 8.2 问题五终点偏差口径

- 5408.3269 m 是“最近目标网格中心 (18750,4750) 到回退终点网格中心
  (15750,250)”的距离（代码 `endpoint_deviation_m` 定义）；
- 题设终点 E(19000,5000) 到 (15750,250) 的直线距离约 5755 m；
- 已核验：目标网格 t=24h 的 A=0.8901、24–72 h 全程 A∈[0.890,0.931]>0.6；
  最近可通行网格确为 (15750,250)（对全部可通行网格逐格最小化验证）；
- 论文措辞已订正为“相对最近目标网格中心偏差 5408 m，与 E 直线距离约
  5755 m”。

### 8.3 问题三最优参数位于域边界

- 网格搜索与差分进化的最优 $(0.015,0.5)$ 分别落在 $C_d$ 上限与 $\alpha$
  下限；NM 三个起点也全部落在边界或谷底；
- 论文已补充说明“正式结果为题给参数域内的最优拟合，而非无约束全局最优”。

### 8.4 问题四跨 κ_s 的 J 不可直接比较

- 不同 $\kappa_s$ 下目标函数含不同权重的平滑惩罚项，跨行 J 不可比；
- 两档优化后站点预测速度 RMS 均为 0.0392 m/s，观测拟合项几乎相同；
- 论文已补充该说明。

### 8.5 其余核验通过项

- GMRES/BiCGSTAB 对照有据可查：`output/problem2/v2_standard_grid/
  200x100_layered_timing.log`，40300×40300 矩阵、GMRES 4.351 s、
  BiCGSTAB 2.147 s、均收敛（info=0）；
- 问题五 Dijkstra 为“空间×576 时间层”展开图（40×20×(576+1) 节点），
  离散模型下标签设定最优性成立；速度与油耗公式与题面一致；
- 测试 83 passed；论文重编译通过（17 页）。

### 8.6 交付说明（main.pdf 锁定）

- 本轮重编译产物为 `paper/main_new.pdf`（17 页、925864 字节，
  含全部订正）；交付 zip `研训188队_数据附件.zip` 内
  `论文_研训188队.pdf` 已更新为同一版本；
- `paper/main.pdf` 因被 WPS Office 打开占用暂未能覆盖替换；
  关闭 WPS 中的 main.pdf 标签后执行
  `Copy-Item paper\main_new.pdf paper\main.pdf -Force` 即可完成替换。

## 9. 2026-08-08 20:10 论文手重写（v2，按用户范文要求扩写）

### 9.1 重写内容

应用户要求，以用户旧论文（2026 杭电校赛 A 题飞轮 PHM 论文）与 2023
研究生数模国赛优秀论文（A 题 WLAN 机理建模，A23102860239/A23105330383）
为范本，对论文做了系统性扩写，核心变化：

1. 每问新增“问题分析”小节，把该问的物理/数值困难与题意结合起来讲透；
2. 模型与算法选型理由从“陈述式”改为“论证式”：列出候选方法、逐一
   说明为何采用/为何放弃，并结合本问题的具体约束（CFL、VP 奇异、
   稀疏观测、时变禁行区、正演成本等）展开；
3. 对比实验全部补充解读：不只列数字，还解释每个数字的物理含义与
   结论边界（如求解器迭代下降的来源、网格一致性、三算法差异的成因、
   时变/静态差异为何只有 0.13% 等）；
4. 问题一新增两张可视化图（五力量级条形图、四模型删项归因对比图，
   数据来自已验证结果，脚本 `tmp/make_p1_figures.py`）；
5. 结论按用户旧论文结构拆分为“主要成果总结 / 模型特色 / 局限性与展望”；
6. 新增两篇真实文献：Hunke & Dukowicz (1997) EVP 模型、
   Cooke & Halsey (1966) 时变最短路，按首次引用顺序插入；
7. 问题二 200×100 的说明改为专业事实陈述（题面定义该网格，本文以
   100×50 为正式标准网格并做一致性检验，200×100 列为后续验证），
   不再采用“超算力”式表述。

### 9.2 产物与验证

- 论文 24 页：`paper/main.pdf`（1126908 字节，2026-08-08 19:49 编译）；
- 全部核验数值口径不变（0.500000/0.208846/636.2/0.484026 等逐项核对）；
- 8 张图全部嵌入（问题一 2 张新增、问题二 3 张、问题三 1 张、
  问题四 1 张、问题五 1 张）；无未定义引用，仅 2 处 <4pt 轻微行溢出；
- 交付 zip 已刷新：`论文_研训188队.pdf` = `paper/main.pdf`，
  并新增 `问题一/` 两张图。

### 9.3 后续可选项（如需继续深化）

- 问题三 NM 三起点收敛轨迹图、目标函数三维曲面图（数据在
  `problem3_nm_partial.json` 与孪生 JSON）；
- 问题四补 κ_s=0.25/1.0 两档并画 L 曲线；
- 问题五关键航路点表格（waypoints 在 `route_result_tau5.json`）。
