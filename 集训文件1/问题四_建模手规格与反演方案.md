# 问题四：初始冰厚场反演与 48 h 预报——建模手规格

生成时间：2026-08-07

## 1. 问题与约束

控制变量为初始冰厚场 $h_i(x,y,0)$；观测为 6 个固定位置在
t=6、12、24 h 共 18 组冰速（表 2，t=24 h 与表 1 相同）。物理约束为
订正后的简化动量方程（M1 三力平衡）与冰厚平流方程。目标：极小化模式
预报冰速与观测偏差，获得更可靠的全场初始冰厚，并预报 t=48 h 的冰速与
冰厚场。

教师订正约束（必须遵守）：

1. 固壁观测点 (20000, 7500) 处海冰无滑移速度为 0，观测误差必须显式建模，
   不得修改观测值或坐标，不得制造非物理的边界冰厚梯度强行拟合；
2. 必须加入背景场与空间平滑正则化；
3. 反演必须使用订正后的正演内核（M1、$\nabla\cdot(h_i\sigma)$）；
4. 正演仍使用问题三得到的 $(C_d^*,\alpha^*)$ 参数对。

## 2. 病态性与降维

题定网格 200×100 共 20000 个初始冰厚自由度，而观测仅 18 个标量约束，
属严重欠定反问题。直接逐格点反演不可行且不稳定。本规格采用低维谱参数化：

$$
h_0(x,y)=
\mathrm{clip}\left[
h_b(x,y)+\sum_{k=0}^{K-1}\sum_{l=0}^{L-1}
c_{kl}\,\varphi_{kl}(x,y),\ 0,\ h_{\max}
\right],
$$

$$
\varphi_{kl}(x,y)=\cos\left(\frac{k\pi x}{L_x}\right)
\cos\left(\frac{l\pi y}{L_y}\right),
$$

其中 $h_b$ 为题面背景场（式 10），$K\times L=4\times3=12$ 个低频系数
（k=0..3、l=0..2），控制全场大尺度修正。低频截断本身就是空间平滑约束，
与梯度正则化互补。$h_{\max}=1.5\ \mathrm m$ 防止非物理厚冰。

## 3. 目标函数

$$
J(\mathbf c)
=J_{\mathrm{obs}}(\mathbf c)
+\lambda_b\,\lVert h_0(\mathbf c)-h_b\rVert_2^2
+\lambda_s\,\lVert\nabla h_0(\mathbf c)\rVert_2^2,
$$

$$
J_{\mathrm{obs}}(\mathbf c)
=\sum_{t\in\{6,12,24\}}\sum_{k=1}^{6}
\frac{(U_{k,t}^{\mathrm{obs}}-U_{k,t}^{\mathrm{mod}}(\mathbf c))^2
+(V_{k,t}^{\mathrm{obs}}-V_{k,t}^{\mathrm{mod}}(\mathbf c))^2}{\sigma_{k,t}^2},
$$

其中 $\sigma_{k,t}$ 为观测误差标准差：内部点 0.01 m/s，固壁点 0.10 m/s
（100 倍方差放大，对应教师提示的观测误差处理）。

正则化权重按尺度归一化并做交叉验证：

$$
\lambda_b=\frac{1}{2\sigma_b^2},\quad
\sigma_b=0.05\ \mathrm m,
$$

$$
\lambda_s=\frac{\lambda_b\,\lVert h_b\rVert_2^2}
{\lVert\nabla h_b\rVert_2^2+\varepsilon}\times\kappa_s,
$$

$\kappa_s\in\{0, 0.25, 0.5, 1.0\}$ 做敏感性对照；$\lambda_b$ 固定。
梯度采用中心差分（并行 12--14 worker），优化器 L-BFGS-B，系数边界
$\lvert c_{kl}\rvert\le 0.3\ \mathrm m$（除 $c_{00}$ 外），
$c_{00}\in[-0.2,0.2]$。收敛判据：目标函数下降或系数变化小于
$10^{-6}$ 且连续 5 次，或达到最大迭代 30 次。

## 4. 正演与观测算子

- 反演正演网格：20×10（与问题三一致），$\Delta t=300\ \mathrm s$，24 h；
- 观测算子：中心速度双线性插值到站点；固壁点按无滑移取 0；
- 抽样时刻 6、12、24 h 的站点模型速度；
- 优化得到系数后，将 $h_0$ 重建场双线性限制/插值到 40×20 做验证，
  再到题定网格预报。

## 5. 输出与验收

1. 背景场直接预报（基线）与反演场预报的 18 点加权残差对比；
2. 反演初始冰厚场与背景场的差异场、最大/平均绝对差；
3. 正则化权重敏感性：$\kappa_s$ 四档的初始场与 24 h 预报差异；
4. t=48 h 冰速场与冰厚场分布图（40×20 验证网格）；
5. 固壁点残差报告（不进入拟合主项）；
6. 全部分析记录在 `output/problem4/`。

## 6. 与问题五的衔接

问题五需要以 $(C_d^*, \alpha^*)$ 与反演 $h_0^*$ 为初边值生成
t∈[24,72] h 逐小时冰密集度场。正式航线规划采用 40×20 或更高网格的
预报场（视算力选择并说明），冰密集度 $A=\mathrm{clip}(h_i/h_{i0},0,1)$。
