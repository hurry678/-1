# 问题一：海冰动量方程的量级分析与瞬时三力平衡简化

## 1. 问题目标与订正后的证据链

问题一要求比较海冰动量方程中五项力的特征量级，提出可检验的简化模型，并说明其适用边界。根据教师提示，二维水平海冰动量方程必须写成

$$
\rho_i h_i\frac{D\mathbf U}{Dt}
=-\rho_i h_i f(\mathbf k\times\mathbf U)
+\nabla\cdot(h_i\boldsymbol\sigma)
+\boldsymbol\tau_{wi}
+\boldsymbol\tau_a,
\tag{1}
$$

其中 $\mathbf U=(U,V)$ 为海冰速度，$h_i$ 为冰厚，$\boldsymbol\sigma$ 为 VP 内应力，$\boldsymbol\tau_{wi}$ 为冰水拖曳应力，$\boldsymbol\tau_a$ 为风应力。五项依次为海冰惯性力、海冰科里奥利力、厚度加权 VP 内力、冰水拖曳力和风应力。

本问采用以下证据链：

$$
\text{量纲订正}
\longrightarrow
\text{五项力量级比较}
\longrightarrow
\text{教师指定的 M1}
\longrightarrow
\text{M0--M1 结构差异评价}
\longrightarrow
\text{$M_I/M_C$ 单项归因}
\longrightarrow
\text{M1 自身数值可信性验证}.
$$

量级分析用于解释教师指定的联合删项在代表性尺度下为何合理，不能代替偏微分方程正演对照。问题一的正式简化模型 M1 同时删除海冰惯性力和海冰科里奥利力，保留厚度加权 VP 内力、冰水拖曳力和风应力。M0--M1 对照量化的是同离散条件下的结构简化差异，不用于重新选择或淘汰 M1；$M_I$、$M_C$ 仅用于删项归因。LHS 如保留，只作补充敏感性筛查，不是正式模型身份的先决条件。

---

## 2. 量纲订正与模型约定

### 2.1 内力必须写成 $\nabla\cdot(h_i\boldsymbol\sigma)$

若 $\boldsymbol\sigma$ 的单位为 Pa，即 $\mathrm{N/m^2}$，则

$$
[\nabla\cdot\boldsymbol\sigma]=\mathrm{N/m^3},
$$

不能与单位为 $\mathrm{N/m^2}$ 的面力项直接相加。二维水平积分后的厚度加权应力 $h_i\boldsymbol\sigma$ 的单位为 $\mathrm{N/m}$，因此

$$
[\nabla\cdot(h_i\boldsymbol\sigma)]=\mathrm{N/m^2},
$$

与惯性、科里奥利、拖曳和风应力的量纲一致。一般情况下

$$
\nabla\cdot(h_i\boldsymbol\sigma)
=h_i\nabla\cdot\boldsymbol\sigma
+\boldsymbol\sigma\nabla h_i
\neq h_i\nabla\cdot\boldsymbol\sigma,
$$

所以不能在变冰厚条件下把 $h_i$ 简单移到散度算子外。

### 2.2 特征尺度与物理参数

题目指定

$$
L_0=20000\ \mathrm{m},\qquad
U_0=0.5\ \mathrm{m/s},\qquad
h_0=1\ \mathrm{m},
$$

故

$$
T_0=\frac{L_0}{U_0}=40000\ \mathrm{s}.
$$

采用

$$
\rho_i=917\ \mathrm{kg/m^3},\qquad
\rho_w=1025\ \mathrm{kg/m^3},
$$

$$
f=1.4\times10^{-4}\ \mathrm{s^{-1}},\qquad
C_d=0.005,
$$

$$
P^*=5000\ \mathrm{Pa/m},\qquad
\boldsymbol\tau_a=(0.12,0.04)\ \mathrm{N/m^2}.
$$

题目未给出独立的海水特征速度，名义量级计算取冰水相对速度

$$
U_r=|\mathbf u-\mathbf U|\sim U_0.
$$

这只是特征尺度假设，不表示海冰绝对速度与冰水相对速度恒等。

---

## 3. 五项力的订正量级

各项均按单位水平面积上的力比较，单位为 $\mathrm{N/m^2}$。

### 3.1 海冰惯性力

取局部加速度和平流加速度的共同特征量级 $U_0^2/L_0$，得到

$$
F_I
\sim\rho_i h_0\frac{U_0^2}{L_0}
=0.0114625.
$$

### 3.2 海冰科里奥利力

$$
F_C
\sim\rho_i h_0fU_0
=0.064190.
$$

### 3.3 冰水拖曳力

冰水拖曳应力取

$$
\boldsymbol\tau_{wi}
=\rho_wC_dU_r(\mathbf u-\mathbf U),
$$

故在 $U_r\sim U_0$ 下

$$
F_D
\sim\rho_wC_dU_0^2
=1.281250.
$$

### 3.4 厚度加权 VP 内力

冰强度为

$$
P=P^*h_i\exp[-20(1-A)].
$$

特征状态下 $h_i=h_0=1\ \mathrm m$、$A_0=1$，所以

$$
P_0=P^*h_0=5000\ \mathrm{Pa}.
$$

由订正后的内力项，VP 特征量级应为

$$
F_\sigma
\sim\frac{h_0P_0}{L_0}.
$$

由于本题 $h_0=1\ \mathrm m$，其数值恰好仍为

$$
F_\sigma
=\frac{1\times5000}{20000}
=0.250000\ \mathrm{N/m^2}.
$$

数值未变不代表公式未变：旧表达 $P_0/L_0$ 在本题仅因 $h_0=1$ 才给出同一数值，不能推广到一般冰厚，也不能用于变冰厚局部分析。

### 3.5 风应力

$$
F_W
=|\boldsymbol\tau_a|
=\sqrt{0.12^2+0.04^2}
=0.126491.
$$

### 3.6 量级汇总与综合删项指标

| 力项 | 特征量级 $\mathrm{N/m^2}$ | 相对于拖曳力 |
|---|---:|---:|
| 海冰惯性力 $F_I$ | 0.011463 | 0.00895 |
| 海冰科里奥利力 $F_C$ | 0.064190 | 0.05010 |
| 厚度加权 VP 内力 $F_\sigma$ | 0.250000 | 0.19512 |
| 冰水拖曳力 $F_D$ | 1.281250 | 1.00000 |
| 风应力 $F_W$ | 0.126491 | 0.09872 |

教师指定同时考察海冰惯性力和海冰科里奥利力的删除，因此采用综合量级指标

$$
\eta_{IC}
=\frac{F_I+F_C}{F_\sigma+F_D+F_W}
=\frac{0.0114625+0.064190}
{0.250000+1.281250+0.126491}
=4.5636\%.
\tag{2}
$$

该比例表明，两项待删除力的合计特征量级约为三项保留力合计特征量级的 $4.5636\%$，为教师指定的瞬时三力平衡 M1 提供尺度层面的解释。M1 的正式身份来自教师订正，不由该比值或后续误差重新决定。

需要特别说明：科里奥利力量级约为风应力的

$$
\frac{F_C}{F_W}\approx50.7\%,
$$

约为 VP 内力的 $25.7\%$、拖曳力的 $5.01\%$。因此不得声称科里奥利力比每一个保留项都远小。式（2）是合并删项与合并保留项之间的量级比较，也不是误差小于 $5\%$ 的数学证明；实际力具有方向并可能相互抵消，净合力尺度可能明显小于各力模长之和。

---

## 4. 订正后的完整模型 M0 与简化模型 M1

### 4.1 M0：完整参考模型

定义物质导数

$$
\frac{D}{Dt}=\frac{\partial}{\partial t}
+U\frac{\partial}{\partial x}
+V\frac{\partial}{\partial y}.
$$

完整模型 M0 为

$$
\rho_i h_i\frac{D\mathbf U}{Dt}
=-\rho_i h_i f(\mathbf k\times\mathbf U)
+\nabla\cdot(h_i\boldsymbol\sigma)
+\boldsymbol\tau_{wi}
+\boldsymbol\tau_a.
\tag{M0}
$$

其分量形式为

$$
\rho_i h_i
\left(
\frac{\partial U}{\partial t}
+U\frac{\partial U}{\partial x}
+V\frac{\partial U}{\partial y}
\right)
=\rho_i h_ifV
+\frac{\partial(h_i\sigma_{xx})}{\partial x}
+\frac{\partial(h_i\sigma_{xy})}{\partial y}
+\tau_{wi,x}+\tau_{a,x},
$$

$$
\rho_i h_i
\left(
\frac{\partial V}{\partial t}
+U\frac{\partial V}{\partial x}
+V\frac{\partial V}{\partial y}
\right)
=-\rho_i h_ifU
+\frac{\partial(h_i\sigma_{yx})}{\partial x}
+\frac{\partial(h_i\sigma_{yy})}{\partial y}
+\tau_{wi,y}+\tau_{a,y}.
$$

M0 是订正后同离散正演对照的参考模型，不等于自然真值。

### 4.2 M1：瞬时 VP--拖曳--风应力三力平衡模型

同时删除海冰惯性力和海冰科里奥利力，得到

$$
0=\nabla\cdot(h_i\boldsymbol\sigma)
+\boldsymbol\tau_{wi}
+\boldsymbol\tau_a.
\tag{M1}
$$

分量形式为

$$
0=
\frac{\partial(h_i\sigma_{xx})}{\partial x}
+\frac{\partial(h_i\sigma_{xy})}{\partial y}
+\rho_wC_dU_r(u-U)
+\tau_{a,x},
$$

$$
0=
\frac{\partial(h_i\sigma_{yx})}{\partial x}
+\frac{\partial(h_i\sigma_{yy})}{\partial y}
+\rho_wC_dU_r(v-V)
+\tau_{a,y},
$$

其中

$$
U_r=\sqrt{(u-U)^2+(v-V)^2}.
$$

M1 在每个真实物理时刻重新求瞬时冰速，再用该冰速推进冰厚和冰水耦合系统。删除海冰惯性项不等于停止整个系统的时间演化。

M0 与 M1 的海水浅水方程均保留海水科里奥利力。M1 删除的仅是海冰动量方程中的科里奥利力，不能通过把全局 $f$ 设为零实现。

### 4.3 正式路线的冻结边界

问题一不再采用“只删除海冰惯性力”的准静态方程，也不设置真实物理时间的启动切换、局部模式切换或切换阈值搜索。若 M1 直接求解困难，可在单个物理时间步内部使用阻尼、伪时间延续、EVP/mEVP、Picard、Newton 或预条件器；这些仅是收敛到 M1 固定点的数值方法，不得改写真实物理方程。

若订正后 M1 相对 M0 的结构差异超过预设 $5\%$ 参考线，应如实报告差异和适用边界。这不表示 M1 未数值收敛，也不改变 M1 的正式模型身份；不得恢复旧的实时切换模型来掩盖结构偏差。

---

## 5. VP 本构、密集度约定与数值正则化

VP 本构保持为

$$
\sigma_{ij}
=2\eta\dot\epsilon_{ij}
+(\zeta-\eta)\dot\epsilon_{kk}\delta_{ij}
-\frac P2\delta_{ij},
$$

其中

$$
\eta=\frac{\zeta}{e^2},\qquad
\zeta=\frac{P}{2\Delta}.
$$

题面同时给出 $A=h_i/h_{i0}\in[0,1]$ 并要求讨论厚冰。为避免 $A>1$ 造成指数项非物理增长，保留如下额外建模约定：

$$
A=\operatorname{clip}\left(\frac{h_i}{h_{i0}},0,1\right),
$$

$$
P=P^*h_i\exp[-20(1-A)].
$$

该截断不是由原方程自动推出，必须在论文假设中明确声明并进行敏感性检查。

初始静止冰场可能使应变率不变量为零。为避免黏度奇异，数值求解可采用

$$
\Delta_\varepsilon
=\sqrt{\Delta^2+\Delta_{\min}^2},
$$

$$
\zeta=\frac{P}{2\Delta_\varepsilon},\qquad
\eta=\frac{\zeta}{e^2},
$$

并在必要时设置 $\zeta\le\zeta_{\max}$。正则化参数必须由收敛和敏感性实验确定；它们是数值处理，不是新的物理删项。

---

## 6. 冰水相对速度尺度的补充敏感性

标准量级始终使用题定 $U_0=0.5\ \mathrm{m/s}$。作为补充，可由风应力与拖曳力的近似平衡估计

$$
\rho_wC_dU_r^2\sim|\boldsymbol\tau_a|,
$$

得到

$$
U_r\sim
\sqrt{\frac{|\boldsymbol\tau_a|}{\rho_wC_d}}
=0.157102\ \mathrm{m/s}.
$$

这里 $U_r$ 是冰水相对速度，只有在 $\mathbf u\approx0$ 的附加假设下，才可用 $U\approx U_r$ 条件性估计海冰惯性力和科里奥利力。采用该附加假设时，各项量级约为：

| 力项 | 条件性量级 $\mathrm{N/m^2}$ |
|---|---:|
| 海冰惯性力 | 0.001132 |
| 海冰科里奥利力 | 0.020169 |
| 冰水拖曳力 | 0.126491 |
| 风应力 | 0.126491 |
| $h_0P_0/L_0$ | 0.250000 |

这组数值只用于速度尺度敏感性说明，不能替代订正后 M0--M1 正演。拖曳调整时间

$$
t_d\sim\frac{\rho_i h_i}{\rho_wC_dU_r}
$$

可用于解释瞬态快慢，但不再用来定义真实时间模型切换；厚冰下 $t_d$ 随 $h_i$ 线性增大，说明快速强迫和启动瞬态仍可能使 M1 偏离 M0。

---

## 7. 初始冰场与局部 VP 量级

初始冰厚场为

$$
h_i(x,y,0)
=0.5+0.3\sin\left(\frac{\pi x}{L_x}\right)
\cos\left(\frac{\pi y}{L_y}\right),
$$

所以

$$
0.2\le h_i(x,y,0)\le0.8\ \mathrm m.
$$

订正后的局部 VP 内力量级必须使用

$$
F_\sigma^{\mathrm{loc}}
\sim\frac{h_iP}{L_\sigma},
\tag{3}
$$

而不是 $P/L_\sigma$。例如：

- $h_i=0.5$ 时，$P\approx0.1135\ \mathrm{Pa}$，若 $L_\sigma=20\ \mathrm{km}$，则 $h_iP/L_\sigma\approx2.84\times10^{-6}\ \mathrm{N/m^2}$；
- $h_i=0.8$ 时，$P\approx73.2626\ \mathrm{Pa}$，若 $L_\sigma=20\ \mathrm{km}$，则 $h_iP/L_\sigma\approx0.002931\ \mathrm{N/m^2}$。

以 $h_i=0.8$ 为例，局部尺度敏感性为：

| $L_\sigma$ | $h_iP/L_\sigma$，单位 $\mathrm{N/m^2}$ |
|---:|---:|
| 20 km | 0.00293 |
| 5 km | 0.01172 |
| 2 km | 0.02931 |
| 1 km | 0.05861 |
| 500 m | 0.11722 |
| 100 m | 0.58610 |

冰缘、边界层、裂隙和挤压带的 $L_\sigma$ 可能远小于海域尺度，因此即使初始冰强度较低，也不能在全域删除 VP 内力。式（3）是特征量级，不是对真实 $\nabla\cdot(h_i\boldsymbol\sigma)$ 的恒等分解。

---

## 8. LHS 稳健性筛选的订正

### 8.1 教师订正前的旧指标状态

旧脚本采用只考察惯性删项的指标

$$
\eta_I^{\mathrm{old}}
=\frac{F_I}{F_C+F_D+F_\sigma+F_W},
$$

并在局部 VP 量级中使用了旧表达 $P/L_\sigma$。由于旧指标没有同时计入待删除的海冰科里奥利力，也没有使用订正后的 $h_iP/L_\sigma$，所有教师订正前的 LHS 通过数与通过率均只作开发归档，不进入当前 M1 证据链。

### 8.2 新的局部综合指标

新 LHS 应统一计算

$$
F_I^{\mathrm{loc}}
=\rho_i h_i\max\left(\frac{U}{T_f},\frac{U^2}{L_U}\right),
$$

$$
F_C^{\mathrm{loc}}=\rho_i h_ifU,
$$

$$
F_D^{\mathrm{loc}}=\rho_wC_dU_r^2,
\qquad
F_\sigma^{\mathrm{loc}}\sim\frac{h_iP}{L_\sigma},
$$

$$
F_W^{\mathrm{loc}}=|\boldsymbol\tau_a^{\mathrm{loc}}|,
$$

并定义

$$
\eta_{IC}^{\mathrm{loc}}
=\frac{F_I^{\mathrm{loc}}+F_C^{\mathrm{loc}}}
{F_\sigma^{\mathrm{loc}}+F_D^{\mathrm{loc}}+F_W^{\mathrm{loc}}}.
\tag{4}
$$

名义参数代入式（4）应回到 $4.5636\%$。可将 $\eta_{IC}^{\mathrm{loc}}<5\%$ 作为 LHS 的候选量级标签，但它只用于识别值得优先正演的参数区，不是物理时间切换判据，也不等价于解误差小于 $5\%$。

新 LHS 如开展，其样本数、设计域、通过样本数和通过率必须重新计算并独立复核；在此之前不填报新通过率。新 LHS 仅是补充敏感性分析，不影响 M1 的正式身份。若各力方向近抵消，基于模长的式（4）仍可能低估删项对净合力的影响，必须由 M0--M1 同离散结构差异评价和 $M_I/M_C$ 单项归因补充。

---

## 9. 碎冰区、厚冰区与极端条件

### 9.1 碎冰区

当 $h_i\to0$ 时，

$$
F_I=O(h_i),\qquad F_C=O(h_i).
$$

在 $A\approx h_i/h_{i0}$ 且其他尺度固定时，

$$
P=P^*h_i\exp[-20(1-A)],
$$

故

$$
F_\sigma^{\mathrm{loc}}
\sim\frac{h_iP}{L_\sigma}
=O(h_i^2)
$$

并带有低密集度指数衰减。与旧方程相比，碎冰区的 VP 内力量级衰减更快。题面风应力和冰水拖曳应力没有显式乘以 $h_i$，因此形式上会逐渐接近拖曳--风应力平衡；但稀疏碎冰也逐渐偏离连续介质 VP 假设，不能仅凭量级把 M1 外推到离散冰块碰撞过程。

当 $h_i=0$ 时不求无物理意义的海冰速度，应使用无冰掩膜把该单元视为开阔水域，并使非活动冰面上的冰水拖曳为零。冰缘传播仍由守恒冰厚输运处理。

### 9.2 厚冰区

当截断后 $A=1$ 时，

$$
P=P^*h_i,
$$

所以

$$
F_\sigma^{\mathrm{loc}}
\sim\frac{h_iP}{L_\sigma}
=\frac{P^*h_i^2}{L_\sigma}.
$$

在固定浓度约定、应变状态和局部尺度下，厚度加权 VP 内力量级近似随 $h_i^2$ 增长，而海冰惯性力和海冰科里奥利力只随 $h_i$ 线性增长。因此厚冰区更不能删除 VP 内力。另一方面，拖曳调整时间随 $h_i$ 增大，快速强迫或初始失衡仍可能放大 M1 与 M0 的动力差异。

“随 $h_i^2$ 增长”是冻结应变尺度下的量级推断，不是对非线性应力散度的精确恒等式。真实 VP 应力还受速度梯度、椭圆屈服曲线、正则化、边界和 $L_\sigma$ 影响。

### 9.3 主要适用边界

需要重点检验的情形包括：

1. 初始冰速与瞬时三力平衡速度差异较大；
2. 风场或海流快速变化，强迫时间尺度接近或短于拖曳调整时间；
3. 冰水相对速度很小，使拖曳力不再保持名义主导量级；
4. 固壁边界层、冰缘、裂隙、挤压带和强剪切区；
5. 碎冰碰撞、破裂和离散冰块运动导致连续介质 VP 假设失效；
6. 各保留力在方向上近似抵消，使删项相对于净合力重新重要；
7. 厚冰、高速度、短局部尺度或高频外部强迫的组合极端情形。

这些情形用于构造验证算例和解释误差，不再用于实时改变模型方程。

---

## 10. 订正后的正式输出、结构参照与删项归因

### 10.1 四模型身份与公平对照

教师订正后，四个模型的身份冻结为：

| 模型 | 海冰惯性 | 海冰科里奥利 | 用途 |
|---|---|---|---|
| M0 | 保留 | 保留 | 同离散完整动力结构参照，不是自然界真值 |
| $M_I$ | 删除 | 保留 | 仅诊断惯性删项的结构贡献 |
| $M_C$ | 保留 | 删除 | 仅诊断海冰科里奥利删项的结构贡献 |
| M1 | 删除 | 删除 | 教师指定的问题二正式海冰动量模型 |

M1 给出题面要求的正式冰速、冰厚和统计量；M0 用于量化 M1 双删项的总结构差异；$M_I$、$M_C$ 只用于 Q2-R2 单项归因，不参与正式模型选择。不得因 $M_I$ 或 $M_C$ 在某个指标上更接近 M0 而让其替代 M1。

四模型除两项海冰开关外，必须使用完全相同的：

- 源码版本、C 型网格和物理时间步；
- 初始条件、边界条件、无冰掩膜和冰缘传播规则；
- VP 本构、$A$ 截断、$\Delta_{\min}$、$\zeta_{\max}$ 及应力离散；
- 风应力、冰水拖曳、海水方程及海水科里奥利力；
- 冰厚守恒输运格式和子步规则；
- 物理残差目标、输出时刻和失败判据。

四模型可以使用各自适配的线性预条件器、阻尼或单步内部伪时间算法，但最终必须分别满足对应的冻结物理方程残差。M0 是同离散参照，不称为真值；M1 自身的数值可信性由残差、守恒、稳定性、容差、时间步和网格收敛独立建立。四模型中的海水科里奥利力均保留，不得将全局 $f$ 置零。

### 10.2 逐物理步误差集合

将 C 网格面速度重构到单元中心：

$$
U^c_{i,j}=\frac12(U_{i-1/2,j}+U_{i+1/2,j}),
$$

$$
V^c_{i,j}=\frac12(V_{i,j-1/2}+V_{i,j+1/2}).
$$

设 M0、M1 的活动冰区为

$$
m_0^n=(h_0^n\ge h_{\min}),\qquad
m_1^n=(h_1^n\ge h_{\min}),
$$

速度误差使用交集 $\Omega_\cap^n=m_0^n\cap m_1^n$，冰厚与冰缘误差使用并集 $\Omega_\cup^n=m_0^n\cup m_1^n$。

24 h 终时刻核心误差定义为

$$
E_U^{24h}
=\frac{\|\mathbf U_1(T)-\mathbf U_0(T)\|_{2,\Omega_\cap}}
{\|\mathbf U_0(T)\|_{2,\Omega_\cap}+\varepsilon},
$$

$$
E_h^{24h}
=\frac{\|h_1(T)-h_0(T)\|_{2,\Omega_\cup}}
{\|h_0(T)\|_{2,\Omega_\cup}+\varepsilon}.
$$

逐物理步时空误差为

$$
E_U^{st}
=\left[
\frac{\sum_{n=1}^{N_t}
\|\mathbf U_1^n-\mathbf U_0^n\|_{2,\Omega_\cap^n}^2}
{\sum_{n=1}^{N_t}
\|\mathbf U_0^n\|_{2,\Omega_\cap^n}^2+\varepsilon}
\right]^{1/2},
$$

$$
E_h^{st}
=\left[
\frac{\sum_{n=1}^{N_t}
\|h_1^n-h_0^n\|_{2,\Omega_\cup^n}^2}
{\sum_{n=1}^{N_t}
\|h_0^n\|_{2,\Omega_\cup^n}^2+\varepsilon}
\right]^{1/2}.
$$

$t=0$ 不计入速度相对误差，但必须保存初值。以上时空误差必须使用每一个物理时间步，不能用稀疏绘图快照代替。

为便于表达结构差异的大小，预先设置 $5\%$ 结构差异参考线：

$$
E_U^{24h}\le5\%,\qquad E_h^{24h}\le5\%,
$$

$$
E_U^{st}\le5\%,\qquad E_h^{st}\le5\%.
\tag{5}
$$

### 10.3 必报辅助指标

除式（5）外，必须报告：

1. 全域中心速度相对误差；
2. 活动冰区中心速度相对误差；
3. 冰质量加权速度误差

   $$
   E_{U,h}
   =\left[
   \frac{\sum h_0|\mathbf U_1-\mathbf U_0|^2}
   {\sum h_0|\mathbf U_0|^2+\varepsilon}
   \right]^{1/2};
   $$

4. 最大中心速度绝对误差 $E_{U,\infty}$ 及其时空位置；
5. 最大冰厚绝对误差 $E_{h,\infty}$ 及其时空位置；
6. 冰区面积相对误差

   $$
   E_A=\frac{|\mathcal A_1-\mathcal A_0|}{\mathcal A_0};
   $$

7. 冰区 Jaccard 指标

   $$
   J_A=\frac{|m_0\cap m_1|}{|m_0\cup m_1|};
   $$

8. 误差能量随时间曲线和空间分布图，用于识别偏差集中在启动期、输运期、冰缘、边界还是强剪切区。

辅助指标用于解释结构差异的时空形态。式（5）的四项 $5\%$ 仅是预先给定的参考线，不是决定 M1 是否作为正式输出模型的淘汰门槛。超过参考线只表明在当前网格、参数和时段下，M1 相对 M0 的结构响应差异较大。

### 10.4 残差、守恒和稳定性验收

M0、$M_I$、$M_C$、M1 每个物理步均须满足相同冻结标准：

| 指标 | 验收阈值 |
|---|---:|
| 海冰物理残差 $\mathcal R_{\rm ice}$ | $\le10^{-3}$ |
| GMRES 实际代数残差 $\mathcal R_{\rm GMRES}$ | $\le10^{-8}$ |
| 冰水外层耦合残差 $\mathcal R_{\rm cpl}$ | $\le10^{-5}$ |
| 海水完整方程残差 $\mathcal R_{\rm water}$ | $\le10^{-8}$ |
| Helmholtz 实际代数残差 | $\le10^{-8}$ |
| 拖曳冲量闭合 $E_{\rm drag}$ | $\le10^{-12}$ |

闭域冰体积为

$$
\mathcal V_i^n=\Delta x\Delta y\sum_{i,j}h_{i,j}^n,
$$

全过程冰体积相对守恒误差要求

$$
E_M
=\max_n\frac{|\mathcal V_i^n-\mathcal V_i^0|}{\mathcal V_i^0}
\le10^{-6}.
$$

海面均值守恒要求

$$
E_\xi
=\max_n|\bar\xi^n-\bar\xi^0|
\le10^{-10}\ \mathrm m.
$$

同时要求：无 NaN/Inf，保存冰厚非负，未收敛物理步数为零，冰厚输运子步 $CFL_i\le0.8$，物理边界通量为零，活动规则一致，冰和海水中心速度不超过用于识别爆解的工程异常阈值 $5\ \mathrm{m/s}$。不得依靠事后全域质量回填或截断掩盖守恒、正性问题。

“单模型数值运行合格”与“M1 相对 M0 的结构差异”必须分开记录：前者说明对应方程在当前离散和容限下求解合格，后者说明双删项引起的同离散结构响应大小。结构差异超过式（5）不等于 M1 数值未收敛。

### 10.5 公平效率指标

M0、M1 在相同硬件、Python 环境、线程数、网格、时间步、输出频率和容差下各独立运行三次，取墙钟时间中位数：

$$
\widetilde T_{M0}=\operatorname{median}(T_{M0}^{(1)},T_{M0}^{(2)},T_{M0}^{(3)}),
$$

$$
\widetilde T_{M1}=\operatorname{median}(T_{M1}^{(1)},T_{M1}^{(2)},T_{M1}^{(3)}).
$$

定义加速比

$$
S=\frac{\widetilde T_{M0}}{\widetilde T_{M1}}.
$$

$S\le1$ 表示无效率收益，$1<S<1.10$ 表示收益有限，$S\ge1.10$ 记为本项目约定的明显效率收益。$1.10$ 是工程表述阈值，不是物理定律或已证明最优值。失败运行不得计时，不得通过减少 M1 输出、放松残差或跳过物理步制造加速。还应报告 Picard、GMRES、冰水耦合迭代总数和每步最大值、重试次数及峰值内存。

Q2-R2 的 6 h 运行受后台争用事件影响，其墙钟数据已经被正式审查排除在效率结论之外。计算效率必须等待隔离环境下的重复计时，不得由本节后述的 6 h 删项归因结果推断。

### 10.6 已签署的 Q2-R2 6 h 紧凑归因结论

已签署的《问题二_Q2-R2建模正式审查》对 $40\times20$、$\Delta t=300\ \mathrm s$、6 h 冻结诊断进行了正式验收。四个模型均完成 72/72 个物理步，58 项测试全部通过，重试为 0；海冰残差、GMRES 残差、冰水耦合残差、海水残差、拖曳闭合、冰量守恒、冰厚非负性和 CFL 均满足冻结门槛。因此，下列数值可作为同离散 6 h 结构归因证据，但不能外推为自然界绝对误差或高网格结论。

相对 M0 的四项核心结构差异为：

| 模型 | $E_U(T)$ | $E_U(st)$ | $E_h(T)$ | $E_h(st)$ |
|---|---:|---:|---:|---:|
| $M_I$ | $4.669\%$ | $9.543\%$ | $2.482\%$ | $2.507\%$ |
| $M_C$ | $10.103\%$ | $8.274\%$ | $10.573\%$ | $6.717\%$ |
| M1 | $11.190\%$ | $12.777\%$ | $11.600\%$ | $7.855\%$ |

这些百分比是模型结构响应差异。M1 四项均超过式（5）的 $5\%$ 参考线，只说明在本冻结 6 h 算例中双删项引起的结构差异不可忽略；它不表示 M1 数值未收敛，也不改变 M1 的正式模型身份。

分变量诊断进一步表明：

- 速度模长严格时空差异中，$M_I$ 为 $9.141\%$，$M_C$ 为 $3.307\%$，说明本算例的速度大小全过程变化对惯性删除更敏感；
- 活动冰区方向均值差异中，$M_C$ 为 $4.043^\circ$，高于 $M_I$ 的 $1.157^\circ$，说明海冰科里奥利删除对运动方向的影响更突出；
- $M_C$ 的冰厚终时和时空结构差异分别为 $10.573\%$、$6.717\%$，明显高于 $M_I$ 的 $2.482\%$、$2.507\%$，呈现方向改变向冰厚输运累积传递的特征；
- M0 中惯性力中位数虽小，但 P95 和最大值达到约 $0.0282$、$0.1162\ \mathrm{N/m^2}$，说明局地和瞬态惯性不能由单一中位数排除；VP 内力的高分位与风应力、拖曳力量级相当，继续支持全域保留 VP 内力。

非加性交互定义为

$$
\mathcal I_X
=\frac{\|X_{\mathrm{M1}}-X_{M_I}-X_{M_C}+X_{\mathrm{M0}}\|_2}
{\|X_{\mathrm{M0}}\|_2+\varepsilon},
\qquad
q_X=\frac{\mathcal I_X}{D_I^X+D_C^X+\varepsilon}.
$$

其中

$$
D_I^X=\frac{\|X_{M_I}-X_{\mathrm{M0}}\|_2}
{\|X_{\mathrm{M0}}\|_2+\varepsilon},
\qquad
D_C^X=\frac{\|X_{M_C}-X_{\mathrm{M0}}\|_2}
{\|X_{\mathrm{M0}}\|_2+\varepsilon}.
$$

| 指标 | $\mathcal I_X$ | $q_X$ | 证据解释 |
|---|---:|---:|---|
| 速度终时 | $4.587\%$ | 0.3105 | 中等交互，不作单项加和或严格排名 |
| 速度时空 | $2.605\%$ | 0.1462 | 交互较弱，可作有限分项比较 |
| 冰厚终时 | $1.827\%$ | 0.1400 | 交互较弱，可作有限分项比较 |
| 冰厚时空 | $1.344\%$ | 0.1457 | 交互较弱，可作有限分项比较 |

因此，6 h 可签署的紧凑归因是：

> 在本 $40\times20$、6 h、冻结初边值和同离散条件下，惯性删除主要放大速度大小的全过程偏离，海冰科里奥利删除主要改变运动方向，并通过输运逐步累积为更明显的冰厚差异。两项同时删除存在非加性交互，不能将两类单项差异直接相加，也不能宣称某一删项在全部变量和时段中全面主导。

该结论只对已验收的 6 h 冻结诊断有效。24 h 四模式归因虽已获准运行，但其数值、交互类别和归因结论必须等待独立正式验收；本文件不预填任何 24 h 结果。

---

## 11. 数值证据状态与后续门禁

教师提示前的均匀冰场启动时间、周期强迫误差、仅删惯性二维粗网格误差、条件混合模型误差及旧加速比，均基于旧内力算子或旧删项定义，只可作为程序开发史料，不进入本问正式证据链。

当前证据状态为：

1. 订正后的 $40\times20$、6 h 四模式 Q2-R2 删项诊断已经通过建模正式审查，允许采用第 10.6 节的紧凑归因结论；
2. 6 h 结果只评价冻结粗网格和冻结时段下的结构差异，不替代 M1 自身的时间步、网格、正则化、守恒和稳定性检验；
3. $40\times20$、24 h 四模式归因已获准运行，但结果尚未完成正式建模验收，本文件不填写任何 24 h 误差、交互分类或归因排序；
4. $M_I$、$M_C$ 在 24 h 归因结束后停止，不进入 $100\times50$ 或 $200\times100$ 正式计算；
5. Q2-R2 的墙钟受后台争用影响，不具备效率证据资格；加速结论等待隔离环境下的正式重复计时；
6. 新 LHS 如继续开展，只作为补充敏感性分析，不是 M1 身份、24 h 归因或高网格正式计算的门槛。

24 h 结果进入本文前必须重新核验：四模式共同配置与源码哈希、全部硬残差和守恒门槛、逐步严格时空指标、方向差异、实际离散力预算、$\mathcal I_X$ 与 $q_X$、失败和重试记录。不得沿用 6 h 的交互类别，也不得从 6 h 归因直接外推 24 h 结论。

---

## 12. 问题一最终结论

按照题目给定的代表性尺度，五项力的特征量级为

$$
F_I=0.011463,\quad
F_C=0.064190,\quad
F_\sigma=0.250000,\quad
F_D=1.281250,\quad
F_W=0.126491
\quad(\mathrm{N/m^2}).
$$

VP 内力的正确特征量级为 $h_0P_0/L_0$；本题因 $h_0=1\ \mathrm m$，数值仍为 $0.25\ \mathrm{N/m^2}$。同时删除的海冰惯性力和海冰科里奥利力，与三项保留力之和的综合比值为

$$
\eta_{IC}=4.5636\%.
$$

因此，问题一的正式简化模型 M1 为

$$
\boxed{
0=\nabla\cdot(h_i\boldsymbol\sigma)
+\boldsymbol\tau_{wi}
+\boldsymbol\tau_a
}.
$$

该模型身份来自教师订正，不因相对 M0 的结构差异超过 $5\%$ 参考线而被淘汰。科里奥利力约为风应力的 $50.7\%$，不能表述为比每个保留项都远小；碎冰区和厚冰区均须用 $h_iP/L_\sigma$ 分析；$A=\operatorname{clip}(h_i/h_{i0},0,1)$ 是额外建模约定。M1 删除的只是海冰科里奥利项，海水科里奥利力在所有模型中继续保留，并可通过海水速度和冰水拖曳间接影响海冰。

已验收的 $40\times20$、6 h 同离散诊断显示：删除惯性对速度模长的全过程变化更敏感，删除海冰科里奥利对运动方向和冰厚输运累积的影响更突出；双删项存在非加性交互，不能直接相加两个单删项差异，也不能宣称某一删项在所有变量和时段中全面主导。M1 相对 M0 的四项结构差异超过 $5\%$ 参考线，只说明本冻结算例中的结构代价不可忽略，不表示 M1 数值未收敛。

问题一由此冻结为单一路线：M1 给出正式题面输出，M0 量化双删项的同离散总结构差异，$M_I/M_C$ 只作单项归因；M1 自身的数值可信性由残差、守恒、稳定性、时间步和网格收敛独立建立。24 h 归因、效率结论和高网格正式结果均等待各自独立验收。
