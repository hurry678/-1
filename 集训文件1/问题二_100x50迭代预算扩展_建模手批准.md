# 问题二 Q2-R4：Picard 重试预算扩展——建模手批准记录

日期：2026-08-08

## 1. 触发事件

100×50 M1 24 h 首轮运行（23:09--01:46）在第 151 步（12.58 h，困难窗口
11.75--12.6 h）失败：standard/reset/robust 三阶段分别以 100/200/300 次
Picard 迭代达到冰残差 0.004882 / 0.002449 / 0.001189，均高于冻结容限
$10^{-3}$。GMRES 线性残差始终满足 $10^{-8}$，守恒与正性检查通过；失败是
非线性 Picard 定点迭代在困难窗口收敛缓慢所致，不是方程或离散错误。

## 2. 单步回放证据

从最后收敛状态（步 150，t=12.5 h）回放步 151：

| 设置 | 结果 | 冰残差 | 耦合残差 | Picard 总数 |
|---|---:|---:|---:|---:|
| 原冻结预算（100/200/300） | 失败 | 0.001189 | -- | 600 |
| reset 300 / robust 500 | 通过 | 0.000968 | 9.86e-6 | 720 |
| reset 300 / robust 500 且松弛 0.25 | 失败 | 0.001787 | -- | 900 |

提高 robust 阶段 Picard 预算至 500 后，同一容限下收敛；降低内层松弛反而恶化。

## 3. 批准内容与依据

将配置默认重试预算改为：

- standard：100 → 100（不变）；
- reset_aitken：200 → 300；
- robust_preconditioner：300 → 500。

耦合迭代上限、残差容限、方程、离散、物理参数、输出与固定点均不变。该改动
只增加非线性迭代预算以到达同一个冻结容限，不改变任何收敛后的解；对已经
收敛的物理步（含 40×20 全部步）结果逐位不变，因此 Q2-R3 已签署的 40×20
等价性与 24 h 复验结论继续有效。

## 4. 生效范围

- 100×50、200×100 标准网格运行；
- 问题三、四、五所有订正内核正演；
- 新测试断言 `tests/test_step_failure_policy.py` 已更新为 300/500。

## 5. 后续门

- 100×50 重跑必须 288/288 步完成、硬检查全过；
- 若困难窗口仍出现未收敛步，按失败工件再做单步回放诊断，必要时在 robust
  阶段启用单物理步内部伪时间/mEVP（冻结清单允许），但不得放松容限。
