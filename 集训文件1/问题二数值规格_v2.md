# 问题二数值规格 v2：教师提示订正后的 M0/M1 统一公式接口

> 文档状态：建模手冻结稿，供编程手修改统一求解器。  
> 冻结日期：2026-08-07。  
> 变更依据：《关于题目的部分提示说明.pdf》与《教师提示订正事件_统一修改计划.md》。  
> 版本关系：本文件在海冰动量方程、VP 内力散度、正式模型层级及相应残差方面取代 `问题二_建模手交付_v1.md`；未被本文件修改的 C 网格、冰水拖曳、海水 IMEX、冰厚守恒输运、边界和失败回滚框架继续沿用。

---

## 1. v2 冻结结论

本次订正属于物理方程级变更。问题二正式路线只保留 M0 和 M1：

1. 完整参考模型 M0 使用订正后的

   $$
   \nabla\cdot(h_i\boldsymbol\sigma);
   $$

2. 正式简化模型 M1 在海冰动量方程中同时删除海冰惯性力和海冰科里奥利力，保留厚度加权 VP 内力、冰水拖曳力和风应力；
3. 海水浅水方程中的科里奥利力在 M0、M1 中均保留；
4. 旧 M2 真实物理时间启动窗口退出正式路线，不再搜索 $t_{\rm sw}$；
5. M1 若求解困难，只能使用单个物理步内部的阻尼、伪时间延续、EVP/mEVP、Newton/Picard 或预条件器改进，不能重新引入真实时间启动窗口；
6. `output/problem2/round2/` 和旧 `round3/` 的结果均基于旧方程，只保留为工程回归记录，不得作为正式数值结论。

本文件冻结的是方程、变量位置、离散通量、残差和验收接口，不授权编程手修改题定物理参数、放松容差或复用旧 M0 场。

## 2. 单位与 C 型网格位置

### 2.1 量纲闭合

海冰动量方程各项统一为单位面积力，单位为 $\mathrm{N/m^2}$：

$$
[\rho_i h_iD\mathbf U/Dt]=\mathrm{N/m^2},
$$

$$
[\boldsymbol\tau_{wi}]=[\boldsymbol\tau_a]=\mathrm{N/m^2}.
$$

VP 应力 $\boldsymbol\sigma$ 的单位为 $\mathrm{Pa}=\mathrm{N/m^2}$，故

$$
[h_i\boldsymbol\sigma]=\mathrm{N/m},
\qquad
[\nabla\cdot(h_i\boldsymbol\sigma)]=\mathrm{N/m^2}.
$$

因此禁止继续使用裸应力散度 $\nabla\cdot\boldsymbol\sigma$ 作为海冰面力。

### 2.2 数组位置和尺寸

数组仍按 `[j,i]` 存储：

| 位置 | 物理尺寸 | 主要变量 |
|---|---:|---|
| 中心 C | $(N_y,N_x)$ | $h_i,\xi,A,P,\zeta,\eta,\dot\epsilon_{xx},\dot\epsilon_{yy},\sigma_{xx},\sigma_{yy},Q_{xx},Q_{yy}$ |
| 东向面 U | $(N_y,N_x+1)$ | $U,u,h^U,\tau_{wi,x},D_{h\sigma,U},R_U$ |
| 北向面 V | $(N_y+1,N_x)$ | $V,v,h^V,\tau_{wi,y},D_{h\sigma,V},R_V$ |
| 角点 K | $(N_y+1,N_x+1)$ | $\dot\epsilon_{xy}^K,\eta^K,h^K,\sigma_{xy}^K,Q_{xy}^K$ |

其中定义厚度加权应力通量

$$
Q_{xx}=h_i\sigma_{xx},\qquad
Q_{yy}=h_i\sigma_{yy},\qquad
Q_{xy}^K=h_i^K\sigma_{xy}^K.
$$

标准网格仍为 $200\times100$，

$$
\Delta x=\Delta y=100\ \mathrm m,
\qquad
\Delta t=300\ \mathrm s,
\qquad
T=24\ \mathrm h.
$$

小规模订正验证依次使用 $20\times10$、$40\times20$；粗网格未通过前不得运行正式 $200\times100$。

## 3. VP 应力本构保持不变

### 3.1 冰强度与浓度约定

继续采用

$$
A_{i,j}
=\operatorname{clip}\left(\frac{h_{i,j}}{h_{i0}},0,1\right),
$$

$$
P_{i,j}
=P^*h_{i,j}\exp[-20(1-A_{i,j})].
$$

$P$ 的单位为 Pa，且其公式中的冰厚不得因外层又出现 $h_i\boldsymbol\sigma$ 而删除。订正后的压力应力通量包含

$$
-\frac12 h_iP,
$$

所以固定浓度关系下其强度尺度可表现为近似 $h_i^2$，这是教师订正后的物理结果，不是重复计数。

### 3.2 应变率、黏度与应力

中心法向应变率、角点剪切应变率、正则化不变量和黏度继续使用 v1 公式：

$$
\dot\epsilon_{xx;i,j}
=\frac{U_{i+1/2,j}-U_{i-1/2,j}}{\Delta x},
$$

$$
\dot\epsilon_{yy;i,j}
=\frac{V_{i,j+1/2}-V_{i,j-1/2}}{\Delta y},
$$

$$
\dot\epsilon_{xy;i+1/2,j+1/2}
=\frac12\left[
\frac{U_{i+1/2,j+1}-U_{i+1/2,j}}{\Delta y}
+\frac{V_{i+1,j+1/2}-V_{i,j+1/2}}{\Delta x}
\right].
$$

$$
\sigma_{xx}
=2\eta\dot\epsilon_{xx}
+(\zeta-\eta)(\dot\epsilon_{xx}+\dot\epsilon_{yy})
-\frac12P,
$$

$$
\sigma_{yy}
=2\eta\dot\epsilon_{yy}
+(\zeta-\eta)(\dot\epsilon_{xx}+\dot\epsilon_{yy})
-\frac12P,
$$

$$
\sigma_{xy}^K=2\eta^K\dot\epsilon_{xy}^K.
$$

这里 $\sigma_{xx},\sigma_{yy},\sigma_{xy}$ 仍为 Pa；厚度加权只在形成应力通量 $Q$ 时进行。

## 4. C 网格上的厚度加权应力通量

### 4.1 中心厚度到角点

内部角点采用四中心算术平均：

$$
h^K_{i+1/2,j+1/2}
=\frac14\left(
h_{i,j}+h_{i+1,j}+h_{i,j+1}+h_{i+1,j+1}
\right).
$$

边界角点只平均相邻有效中心。等价地，可先对中心冰厚作偶延拓，再应用同一四点平均：

$$
h_{i,-1}=h_{i,0},\quad
h_{i,N_y}=h_{i,N_y-1},
$$

$$
h_{-1,j}=h_{0,j},\quad
h_{N_x,j}=h_{N_x-1,j}.
$$

因此下边界内部角点满足

$$
h^K_{i+1/2,-1/2}
=\frac12(h_{i,0}+h_{i+1,0}),
$$

角落取唯一相邻中心值。

### 4.2 厚度加权应力通量

中心法向通量：

$$
Q_{xx;i,j}=h_{i,j}\sigma_{xx;i,j},
$$

$$
Q_{yy;i,j}=h_{i,j}\sigma_{yy;i,j}.
$$

角点剪切通量：

$$
Q_{xy;i+1/2,j+1/2}
=h^K_{i+1/2,j+1/2}
\sigma_{xy;i+1/2,j+1/2}.
$$

先按冻结 VP 公式计算 $\sigma_{xy}^K$，再乘同一角点的 $h^K$。禁止把中心 $h\sigma_{xy}$ 分别插值后混用不同位置。

### 4.3 散度回到 U 面

对内部东向面 $(i+1/2,j)$：

$$
D_{h\sigma,U;i+1/2,j}
=\frac{Q_{xx;i+1,j}-Q_{xx;i,j}}{\Delta x}
+\frac{
Q_{xy;i+1/2,j+1/2}
-Q_{xy;i+1/2,j-1/2}
}{\Delta y}.
$$

即

$$
D_{h\sigma,U}
=\left[\nabla_h\cdot(h_i\boldsymbol\sigma)\right]_U.
$$

### 4.4 散度回到 V 面

对内部北向面 $(i,j+1/2)$：

$$
D_{h\sigma,V;i,j+1/2}
=\frac{
Q_{xy;i+1/2,j+1/2}
-Q_{xy;i-1/2,j+1/2}
}{\Delta x}
+\frac{Q_{yy;i,j+1}-Q_{yy;i,j}}{\Delta y}.
$$

即

$$
D_{h\sigma,V}
=\left[\nabla_h\cdot(h_i\boldsymbol\sigma)\right]_V.
$$

物理边界面海冰速度由无滑移 Dirichlet 条件直接给定为零，不在边界面组装冰动量未知量。边界角点的 $\sigma_{xy}^K$ 继续使用海冰切向速度奇延拓，$h^K$ 使用上述偶延拓/单边平均。

### 4.5 明确禁止的错误实现

一般有

$$
\nabla_h\cdot(h_i\boldsymbol\sigma)
\neq
h_i^{\rm face}(\nabla_h\cdot\boldsymbol\sigma).
$$

编程手不得：

1. 先调用旧 `stress_divergence()` 再乘 $h^U$ 或 $h^V$；
2. 只给黏性应力乘厚度、仍使用裸压力梯度；
3. 在压力 $P=P^*h_i\exp[-20(1-A)]$ 中删除冰厚；
4. 用中心厚度直接乘角点 $\sigma_{xy}$ 而不先插值到角点；
5. 让物理残差和 Picard 矩阵使用不同的厚度加权算子。

### 4.6 Picard 线性化的矩阵接口

在 Picard 第 $k$ 轮冻结 $h_i^n,\zeta^k,\eta^k$ 后，中心法向黏性系数改为

$$
K_+^C=h_i^n(\zeta^k+\eta^k),
\qquad
K_-^C=h_i^n(\zeta^k-\eta^k),
$$

角点剪切系数改为

$$
K_s^K=2h_i^{K,n}\eta^{K,k}.
$$

因此四个黏性矩阵块冻结为

$$
B_{UU}
=D_x^{C\to U}\operatorname{diag}(K_+^C)E_{xx}^U
+D_y^{K\to U}\operatorname{diag}(K_s^K)E_{xy}^U,
$$

$$
B_{UV}
=D_x^{C\to U}\operatorname{diag}(K_-^C)E_{yy}^V
+D_y^{K\to U}\operatorname{diag}(K_s^K)E_{xy}^V,
$$

$$
B_{VU}
=D_x^{K\to V}\operatorname{diag}(K_s^K)E_{xy}^U
+D_y^{C\to V}\operatorname{diag}(K_-^C)E_{xx}^U,
$$

$$
B_{VV}
=D_x^{K\to V}\operatorname{diag}(K_s^K)E_{xy}^V
+D_y^{C\to V}\operatorname{diag}(K_+^C)E_{yy}^V.
$$

压力应力通量在中心定义为

$$
Q_P^C=-\frac12h_i^nP^k,
$$

右端压力贡献必须为

$$
D_x^{C\to U}Q_P^C,
\qquad
D_y^{C\to V}Q_P^C.
$$

这取代旧实现中的 $D_x(-P/2)$ 和 $D_y(-P/2)$。

## 5. M0、M1 连续方程

### 5.1 两个独立海冰开关

为防止误删海水科里奥利，定义两个只作用于海冰方程的标量：

$$
\chi_I\in\{0,1\},\qquad
\chi_C\in\{0,1\},
$$

分别控制海冰惯性项和海冰科里奥利项。

| 模型 | $\chi_I$ | $\chi_C$ | 含义 |
|---|---:|---:|---|
| M0 | 1 | 1 | 订正后的完整海冰动力学参考模型 |
| M1 | 0 | 0 | 教师指定的瞬时 VP--拖曳--风应力三力平衡模型 |

旧 M2 不进入正式驱动、选模和论文结果表。

### 5.2 统一海冰方程

统一写为

$$
\chi_I\rho_i h_i
\left(
\frac{\partial\mathbf U}{\partial t}
+\mathbf U\cdot\nabla\mathbf U
\right)
=-\chi_C\rho_i h_if(\mathbf k\times\mathbf U)
+\nabla\cdot(h_i\boldsymbol\sigma)
+\boldsymbol\tau_{wi}
+\boldsymbol\tau_a.
$$

东向分量：

$$
\chi_I\rho_i h_i
\left(
\frac{\partial U}{\partial t}
+U\frac{\partial U}{\partial x}
+V\frac{\partial U}{\partial y}
\right)
=\chi_C\rho_i h_ifV
+\frac{\partial(h_i\sigma_{xx})}{\partial x}
+\frac{\partial(h_i\sigma_{xy})}{\partial y}
+\tau_{wi,x}+\tau_{a,x}.
$$

北向分量：

$$
\chi_I\rho_i h_i
\left(
\frac{\partial V}{\partial t}
+U\frac{\partial V}{\partial x}
+V\frac{\partial V}{\partial y}
\right)
=-\chi_C\rho_i h_ifU
+\frac{\partial(h_i\sigma_{xy})}{\partial x}
+\frac{\partial(h_i\sigma_{yy})}{\partial y}
+\tau_{wi,y}+\tau_{a,y}.
$$

### 5.3 M0 完整参考模型

M0 取 $(\chi_I,\chi_C)=(1,1)$：

$$
\rho_i h_i\frac{D\mathbf U}{Dt}
=-\rho_i h_if(\mathbf k\times\mathbf U)
+\nabla\cdot(h_i\boldsymbol\sigma)
+\boldsymbol\tau_{wi}
+\boldsymbol\tau_a.
$$

M0 与 M1 使用相同网格、时间步、VP 正则化、边界、拖曳、海水、冰厚输运和目标残差。M0 是同离散模型误差参照，不得称为真值；标准网格比较前仍须做 M0 的时间步和正则化敏感性检查。

### 5.4 M1 瞬时三力平衡模型

M1 取 $(\chi_I,\chi_C)=(0,0)$：

$$
0=
\nabla\cdot(h_i\boldsymbol\sigma)
+\boldsymbol\tau_{wi}
+\boldsymbol\tau_a.
$$

即

$$
0=D_{h\sigma,U}+\tau_{wi,x}+\tau_{a,x},
$$

$$
0=D_{h\sigma,V}+\tau_{wi,y}+\tau_{a,y}.
$$

M1 仍在每个真实物理时刻重新求瞬时冰速，并用该冰速推进冰厚；海水仍按真实时间浅水方程推进。删除海冰惯性不等于停止整个耦合系统的时间演化。

## 6. 海冰与海水科里奥利项的范围

### 6.1 冻结范围表

| 方程 | M0 | M1 | 备注 |
|---|---|---|---|
| 海冰东向 $+\rho_i h_ifV$ | 保留 | 删除 | 仅由 `ice_coriolis_weight` 控制 |
| 海冰北向 $-\rho_i h_ifU$ | 保留 | 删除 | 与东向同时开关 |
| 海水东向 $+fv^{(u)}$ | 保留 | 保留 | 不能因 M1 将全局 $f$ 设为 0 |
| 海水北向 $-fu^{(v)}$ | 保留 | 保留 | 继续采用已批准的局部隐式/半隐式处理 |
| 冰水二次拖曳 | 保留 | 保留 | 相对速度仍包含两个水平分量 |

### 6.2 建议配置接口

正式配置应至少区分：

```text
ice_inertia_weight
ice_coriolis_weight
ocean_coriolis_enabled = true
```

禁止用一个全局 `coriolis=0` 实现 M1。题定 $f=1.4\times10^{-4}\ \mathrm{s^{-1}}$ 必须继续传入海水求解器。

若伪时间、EVP 或预条件器内部使用辅助旋转块，只要最终固定点严格满足 M1 物理残差即可；该辅助项不得写入真实方程、真实时间或结果解释。

## 7. 海水方程与冰水拖曳不变

冰所受界面拖曳仍为

$$
\boldsymbol\tau_{wi}
=\rho_wC_d|\mathbf u-\mathbf U|(\mathbf u-\mathbf U),
$$

海水使用相反作用。海水 C 网格方程保持

$$
\frac{\xi^{n+1}-\xi^n}{\Delta t}
+h_wD_h\mathbf u^{n+1}=0,
$$

$$
\frac{u^{n+1}-u^n}{\Delta t}+\mathcal A_u
=-gG_x\xi^{n+1}
+fv^{(u),n+1}
-\frac{\tau_{wi,x}}{\rho_wh_w},
$$

$$
\frac{v^{n+1}-v^n}{\Delta t}+\mathcal A_v
=-gG_y\xi^{n+1}
-fu^{(v),n+1}
-\frac{\tau_{wi,y}}{\rho_wh_w}.
$$

冰、水双方必须复用同一外层轮次、同一面位置和同一数值对象中的 $\tau_{wi,x},\tau_{wi,y}$。无冰面规则、逻辑“或”活动面和等大反向关系继续沿用。

为隔离本次方程订正，$20\times10$ 和 $40\times20$ 订正回归允许继续使用当前已验证的显式一阶海水平流作为工程基线；若正式标准网格升级为 MUSCL，必须在 M0、M1 上同时升级并单独完成误差、守恒和计时复验，不能与本次方程修改混为一次变更。

## 8. 海冰动量半离散与物理残差

### 8.1 M0 惯性离散

M0 延续后向欧拉与 Picard 冻结输运速度的一阶隐式迎风：

$$
I_U
=\chi_I\rho_i h^U
\left[
\frac{U^{n+1}-U^n}{\Delta t}
+\mathcal A_U^{n+1}
\right],
$$

$$
I_V
=\chi_I\rho_i h^V
\left[
\frac{V^{n+1}-V^n}{\Delta t}
+\mathcal A_V^{n+1}
\right].
$$

M1 中 $I_U=I_V=0$，不得保留冰动量平流。

### 8.2 海冰科里奥利分项

$$
C_U
=\chi_C\rho_i h^UfV^{(U),n+1},
$$

$$
C_V
=-\chi_C\rho_i h^VfU^{(V),n+1}.
$$

M1 中 $C_U=C_V=0$。

### 8.3 统一物理残差

所有活动内部面上定义

$$
R_U
=I_U-\left[
C_U+D_{h\sigma,U}
+\tau_{wi,x}+\tau_{a,x}
\right],
$$

$$
R_V
=I_V-\left[
C_V+D_{h\sigma,V}
+\tau_{wi,y}+\tau_{a,y}
\right].
$$

逐点尺度为

$$
S_U
=|I_U|+|C_U|+|D_{h\sigma,U}|
+|\tau_{wi,x}|+|\tau_{a,x}|,
$$

$$
S_V
=|I_V|+|C_V|+|D_{h\sigma,V}|
+|\tau_{wi,y}|+|\tau_{a,y}|.
$$

全域归一化海冰物理残差冻结为

$$
\mathcal R_{\rm ice}
=\frac{
\left[
\sum_{\Omega_U}R_U^2
+\sum_{\Omega_V}R_V^2
\right]^{1/2}
}{
\left[
\sum_{\Omega_U}S_U^2
+\sum_{\Omega_V}S_V^2
\right]^{1/2}
+\varepsilon_F
}.
$$

$\Omega_U,\Omega_V$ 只含活动内部冰面。矩阵组装、最终物理残差和输出诊断必须调用同一厚度加权应力通量算子。

### 8.4 线性残差

每次 GMRES 求解记录

$$
\mathcal R_{\rm GMRES}
=\frac{\|\mathbf A\mathbf x-\mathbf b\|_2}
{\|\mathbf b\|_2+\varepsilon_b}.
$$

只记录求解器内部退出标志不足以证明通过。

## 9. 冰水耦合、海水和拖曳闭合残差

### 9.1 外层耦合残差

第 $k$ 轮定义

$$
\mathcal R_{\rm cpl}
=\max\left\{
\frac{\|\mathbf U^k-\mathbf U^{k-1}\|_2}
{\|\mathbf U^k\|_2+\varepsilon_U},
\frac{\|\mathbf u^k-\mathbf u^{k-1}\|_2}
{\|\mathbf u^k\|_2+\varepsilon_u},
\frac{\|\boldsymbol\tau_{wi}^k-\boldsymbol\tau_{wi}^{k-1}\|_2}
{\|\boldsymbol\tau_{wi}^k\|_2+\varepsilon_\tau},
\frac{\|\xi^k-\xi^{k-1}\|_2}
{\|\xi^k\|_2+\varepsilon_\xi}
\right\}.
$$

冰速范数只取活动面。

### 9.2 海水完整方程残差

海水科里奥利继续进入物理残差：

$$
r_\xi
=\frac{\xi^{n+1}-\xi^n}{\Delta t}
+h_wD_h\mathbf u^{n+1},
$$

$$
r_u
=\frac{u^{n+1}-u^n}{\Delta t}
+\mathcal A_u+gG_x\xi^{n+1}
-fv^{(u),n+1}
+\frac{\tau_{wi,x}}{\rho_wh_w},
$$

$$
r_v
=\frac{v^{n+1}-v^n}{\Delta t}
+\mathcal A_v+gG_y\xi^{n+1}
+fu^{(v),n+1}
+\frac{\tau_{wi,y}}{\rho_wh_w}.
$$

分别用各自完整项尺度归一化，最终

$$
\mathcal R_{\rm water}
=\max(\mathcal R_\xi,\mathcal R_u,\mathcal R_v).
$$

海水东、北动量残差分母必须保留 $f\|v^{(u)}\|_2$、$f\|u^{(v)}\|_2$，不能因 M1 删除海冰科里奥利而删除。

### 9.3 实际拖曳冲量闭合

从冰、水方程实际施加的数组计算

$$
\mathbf I_{\rm ice}
=\Delta t\,\Delta x\Delta y
\left(
\sum_{\Omega_U}\tau_{wi,x},
\sum_{\Omega_V}\tau_{wi,y}
\right),
$$

$$
\mathbf I_{\rm water}
=\Delta t\,\Delta x\Delta y
\left(
\sum_{\Omega_U}F_{w,x},
\sum_{\Omega_V}F_{w,y}
\right),
$$

其中实际水侧面力应满足 $F_{w,x}=-\tau_{wi,x}$、$F_{w,y}=-\tau_{wi,y}$。闭合指标为

$$
E_{\rm drag}
=\frac{\|\mathbf I_{\rm ice}+\mathbf I_{\rm water}\|_2}
{\|\mathbf I_{\rm ice}\|_2+\varepsilon_I}.
$$

不得先人为构造 $\mathbf I_{\rm water}=-\mathbf I_{\rm ice}$ 再验证恒等式。建议同时记录逐面的 L1 闭合误差作为更严格辅助指标，但论文中的冻结指标使用上式。

## 10. 残差阈值

M0、M1 使用相同目标容差。允许最大迭代数、预条件器或 M1 内部伪时间算法不同，但不得放松以下阈值：

| 指标 | 冻结阈值 |
|---|---:|
| 海冰物理残差 $\mathcal R_{\rm ice}$ | $\le10^{-3}$ |
| GMRES 实际代数残差 $\mathcal R_{\rm GMRES}$ | $\le10^{-8}$ |
| 冰水外层耦合残差 $\mathcal R_{\rm cpl}$ | $\le10^{-5}$ |
| 海水完整方程残差 $\mathcal R_{\rm water}$ | $\le10^{-8}$ |
| Helmholtz 实际代数残差 | $\le10^{-8}$ |
| 拖曳冲量闭合 $E_{\rm drag}$ | $\le10^{-12}$ |

每个物理步均须满足。达到批准的重试上限仍未满足时，整次配置标记为失败并中止；不得保存未收敛状态继续推进。

## 11. 守恒、正性与稳定性标准

### 11.1 冰体积守恒

闭域冰体积

$$
\mathcal V_i^n
=\Delta x\Delta y\sum_{i,j}h_{i,j}^n.
$$

初始体积仍为

$$
\mathcal V_i^0=10^8\ \mathrm{m^3}.
$$

全过程最大相对误差

$$
E_M
=\max_n
\frac{|\mathcal V_i^n-\mathcal V_i^0|}
{\mathcal V_i^0}
\le10^{-6}.
$$

不得依靠非守恒的全域质量回填满足该标准。

### 11.2 海面均值守恒

闭域浅水初始平均海面为零，定义

$$
E_\xi
=\max_n|\bar\xi^n-\bar\xi^0|,
\qquad
\bar\xi^n=\frac1{N_xN_y}\sum_{i,j}\xi_{i,j}^n.
$$

工程冻结阈值为

$$
E_\xi\le10^{-10}\ \mathrm m.
$$

### 11.3 冰厚、CFL 和异常状态

每个正式配置必须同时满足：

1. 所有保存状态无 NaN、Inf；
2. 保存的冰厚满足 $h_i\ge0$；若正性通量计算出现低于 $-10^{-12}\ \mathrm m$ 的中间值，直接判为失败，不得靠截断掩盖；
3. 冰厚输运每个子步

   $$
   CFL_i
   =\max|U|\frac{\Delta t_s}{\Delta x}
   +\max|V|\frac{\Delta t_s}{\Delta y}
   \le0.8;
   $$

4. 冰厚物理边界通量严格为零；
5. 海水法向边界通量严格为零；
6. 未收敛物理步数为零；
7. 冰、海水中心速度均不超过工程异常阈值 $5\ \mathrm{m/s}$；该阈值只用于发现爆解，不代表预期物理速度；
8. 无冰面拖曳严格为零，冰/无冰交界面仍允许冰缘向无冰单元传播。

## 12. M1 相对 M0 的精度标准

### 12.1 比较原则

M1 只与同离散 M0 比较。两者必须使用：

- 相同 C 网格和物理时间步；
- 相同 VP、拖曳、海水、冰厚输运和边界；
- 相同物理残差目标；
- 相同输出时间集合；
- 相同源码版本。

M0 仍含离散、正则化和迭代误差，不称为真值。模型比较前应先验证 M0 对 $\Delta t$、网格和正则化参数的敏感性已降低。

### 12.2 中心速度与冰区集合

面速度重构到中心：

$$
U^c_{i,j}=\frac12(U_{i-1/2,j}+U_{i+1/2,j}),
$$

$$
V^c_{i,j}=\frac12(V_{i,j-1/2}+V_{i,j+1/2}).
$$

参考和简化冰区为

$$
m_0=(h_0\ge h_{\min}),
\qquad
m_1=(h_1\ge h_{\min}).
$$

速度使用活动交集

$$
\Omega_\cap=m_0\cap m_1,
$$

冰厚和冰缘使用并集

$$
\Omega_\cup=m_0\cup m_1.
$$

### 12.3 四项核心误差

终时刻速度和冰厚误差：

$$
E_U^{24h}
=\frac{\|\mathbf U_1(T)-\mathbf U_0(T)\|_{2,\Omega_\cap}}
{\|\mathbf U_0(T)\|_{2,\Omega_\cap}+\varepsilon},
$$

$$
E_h^{24h}
=\frac{\|h_1(T)-h_0(T)\|_{2,\Omega_\cup}}
{\|h_0(T)\|_{2,\Omega_\cup}+\varepsilon}.
$$

逐物理步时空误差：

$$
E_U^{st}
=\left[
\frac{
\sum_{n=1}^{N_t}
\|\mathbf U_1^n-\mathbf U_0^n\|_{2,\Omega_\cap^n}^2
}{
\sum_{n=1}^{N_t}
\|\mathbf U_0^n\|_{2,\Omega_\cap^n}^2+\varepsilon
}
\right]^{1/2},
$$

$$
E_h^{st}
=\left[
\frac{
\sum_{n=1}^{N_t}
\|h_1^n-h_0^n\|_{2,\Omega_\cup^n}^2
}{
\sum_{n=1}^{N_t}
\|h_0^n\|_{2,\Omega_\cup^n}^2+\varepsilon
}
\right]^{1/2}.
$$

$t=0$ 不计入速度相对误差，但必须保存初值。禁止用少量绘图快照代替逐步误差集合。

### 12.4 精度通过条件

M1 达到“相对 M0 的 5% 精度目标”必须同时满足

$$
E_U^{24h}\le5\%,\qquad
E_h^{24h}\le5\%,
$$

$$
E_U^{st}\le5\%,\qquad
E_h^{st}\le5\%.
$$

若 M1 未通过，仍按教师指定方程完成 M1 正演并如实报告误差、适用边界和局限；不得重新引入旧 M2 真实时间窗口以掩盖偏差。

### 12.5 必报辅助误差

除四项核心误差外，必须报告：

1. 全域中心速度相对误差；
2. 冰质量加权速度误差

   $$
   E_{U,h}
   =\left[
   \frac{\sum h_0|\mathbf U_1-\mathbf U_0|^2}
   {\sum h_0|\mathbf U_0|^2+\varepsilon}
   \right]^{1/2};
   $$

3. 最大中心速度绝对误差

   $$
   E_{U,\infty}=\max_{i,j}|\mathbf U_1^c-\mathbf U_0^c|;
   $$

4. 最大冰厚绝对误差 $E_{h,\infty}=\max|h_1-h_0|$；
5. 冰区面积相对误差和 Jaccard 指标

   $$
   E_A=\frac{|\mathcal A_1-\mathcal A_0|}{\mathcal A_0},
   \qquad
   J_A=\frac{|m_0\cap m_1|}{|m_0\cup m_1|};
   $$

6. 误差能量随时间曲线，用于判断偏差发生在启动期、后续输运期、冰缘还是边界附近。

这些辅助指标用于解释误差结构，不单独替代四项 5% 硬门槛。

## 13. 效率标准

### 13.1 公平计时

M0、M1 在相同硬件、Python 环境、线程数、输出频率、网格、时间步和容差下独立运行。每个配置重复 3 次，使用墙钟时间中位数：

$$
\widetilde T_{M0}=\operatorname{median}(T_{M0}^{(1)},T_{M0}^{(2)},T_{M0}^{(3)}),
$$

$$
\widetilde T_{M1}=\operatorname{median}(T_{M1}^{(1)},T_{M1}^{(2)},T_{M1}^{(3)}).
$$

加速比定义为

$$
S=\frac{\widetilde T_{M0}}{\widetilde T_{M1}}.
$$

失败运行不得进入计时汇总；不得减少 M1 输出、放松 M1 残差或跳过物理步制造加速。

### 13.2 效率判断

- $S\le1$：无效率收益；
- $1<S<1.10$：有轻微收益，但只能表述为“收益有限”；
- $S\ge1.10$：达到本项目约定的“明显效率收益”门槛。

$S>1$ 是进入效率可行集的最低条件；$1.10$ 是竞赛工程表述阈值，不是物理定律或已证明最优门槛。

同时报告：

- Picard/非线性迭代总数与每步最大值；
- GMRES 总迭代数与每步最大值；
- 冰水耦合总迭代数与每步最大值；
- 重试物理步数；
- 峰值内存（若可得）。

### 13.3 精度与效率的处理规则

1. M1 稳定、四项精度通过且 $S\ge1.10$：可进入 $100\times50$ 标准前复核；
2. M1 精度通过但 $S<1.10$：保持 M1 物理方程不变，优先优化预条件器、阻尼、伪时间或 mEVP；
3. M1 精度不通过：仍完成教师指定 M1 输出并报告误差边界，不恢复真实时间 M2；
4. 任何求解器优化均须维持同一 M1 固定点和同一物理残差；
5. 粗网格未通过稳定性、守恒和残差硬条件，不进入 $200\times100$。

## 14. 单次运行的总验收门

一个 M0 或 M1 配置只有同时满足下列条件才记为 `passed`：

1. 完成全部计划物理步，未收敛步数为 0；
2. 所有状态无 NaN、Inf、负冰厚和异常爆速；
3. $\mathcal R_{\rm ice}\le10^{-3}$；
4. $\mathcal R_{\rm GMRES}\le10^{-8}$；
5. $\mathcal R_{\rm cpl}\le10^{-5}$；
6. $\mathcal R_{\rm water}\le10^{-8}$；
7. Helmholtz 残差 $\le10^{-8}$；
8. $E_{\rm drag}\le10^{-12}$ 且非活动面拖曳精确为零；
9. 冰体积全过程最大相对误差 $E_M\le10^{-6}$；
10. 海面均值误差 $E_\xi\le10^{-10}\ \mathrm m$；
11. 冰厚子步 CFL 不超过 0.8；
12. 运行开始、结束及聚合时的源码哈希一致。

M1 的“运行通过”与“相对 M0 精度通过”分开记录。前者说明数值求解合格，后者说明简化误差达到 5% 目标。

## 15. 编程手必须增加或更新的测试

### 15.1 方程与模式

1. M0 的 `ice_inertia_weight=1`、`ice_coriolis_weight=1`；
2. M1 的两个海冰权重均为 0；
3. M0、M1 的海水科里奥利均开启；
4. M0、M1 除两个海冰权重外使用同一物理配置；
5. 正式运行驱动不再枚举旧 M2 或 $t_{\rm sw}$。

### 15.2 厚度加权应力散度

1. **常冰厚回归**：当 $h_i\equiv1\ \mathrm m$ 时，新算子退化为旧 $\nabla_h\cdot\sigma$；
2. **常任意厚度回归**：当 $h_i\equiv h_0$ 时，结果为 $h_0\nabla_h\cdot\sigma$；
3. **常应力、变冰厚**：$\sigma$ 常量而 $h_i$ 变化时，散度一般非零；
4. **常厚度加权应力**：$Q_{xx},Q_{yy},Q_{xy}$ 常量时散度为零；
5. **乘积规则辨识**：构造场验证新算子一般不等于 $h^{\rm face}\nabla_h\cdot\sigma$；
6. **变冰厚制造解**：解析给定 $h_i(x,y)$ 和 $\sigma(x,y)$，内部网格误差随加密稳定下降，目标阶约 2；
7. **角点剪切通量**：验证 $h^K\sigma_{xy}^K$ 的位置、符号、四点平均和边界单边平均；
8. **压力通量**：验证矩阵右端使用 $D(-hP/2)$，而不是 $h^{\rm face}D(-P/2)$；
9. **矩阵--残差一致性**：同一候选速度代入线性矩阵和物理算子，厚度加权部分一致；
10. **零冰/薄冰**：厚度加权应力随厚度衰减，不除以极小冰质量，不阻断冰缘输运。

### 15.3 既有测试继续保留

- C 网格形状、插值和速度面位置；
- 海冰奇延拓、海水偶延拓、冰厚零通量；
- 冰缘推进和冰厚守恒；
- 海水 IMEX、科里奥利和三个完整方程残差；
- 同一拖曳对象和实际冲量闭合；
- 失败回滚、重试、哈希和逐步时空误差。

旧“均匀应力散度为零”测试必须增加“冰厚也均匀”或“厚度加权应力均匀”的前提。

## 16. 恢复计算的固定顺序

1. 编程手只修改代码和测试，不复用旧 M0/M1 场；
2. 全部单元测试通过；
3. $20\times10$、0.5 h 的 M0/M1 冒烟；
4. $40\times20$、6 h 的 M0/M1 回归，检查残差、守恒、拖曳闭合和迭代量；
5. $40\times20$、24 h 的新 M0/M1 完整对照；
6. 三次独立重复计时取中位数；
7. 根据精度和效率决定是优化 M1 求解器，还是进入 $100\times50$；
8. 粗网格全部硬条件通过后，才讨论 $200\times100$ 正式输出。

不再执行真实物理时间启动窗口搜索。

## 17. v2 冻结检查表

编程手开始修改前，以下内容视为已冻结：

- [x] M0 使用 $\nabla\cdot(h_i\sigma)$，保留海冰惯性和海冰科里奥利；
- [x] M1 使用瞬时三力平衡，同时删除海冰惯性和海冰科里奥利；
- [x] 海水科里奥利在 M0、M1 中均保留；
- [x] VP 压力 $P=P^*h_i\exp[-20(1-A)]$ 不变；
- [x] 中心计算 $h_i\sigma_{xx},h_i\sigma_{yy}$，角点计算 $h_i^K\sigma_{xy}^K$；
- [x] 散度采用厚度加权应力通量差，不采用面厚乘旧散度；
- [x] Picard 矩阵、压力右端和物理残差使用同一订正算子；
- [x] M2 退出正式路线；
- [x] 残差、守恒、5% 精度和重复计时效率标准已冻结；
- [x] 旧结果只作为工程回归记录。

编程手不得在实现过程中自行改换离散位置、恢复 M2、关闭海水科里奥利或放松验收门槛。若发现本规格与代码结构存在无法直接实现的冲突，应停止扩展并单独列出，不得自行更换物理模型。
